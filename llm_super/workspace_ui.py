"""Unified conversation workspace served without a frontend build step."""

PAGE = r'''<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>Workspace · llm-super</title>
<link rel="icon" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 32 32'%3E%3Crect width='32' height='32' rx='9' fill='%23eef2f8'/%3E%3Cpath d='M9 14h5l-2-3 7 5h4l-3 5-4-3h-5z' fill='%230c0f14'/%3E%3C/svg%3E">
<style>
:root{color-scheme:dark;--page:#0c0f14;--panel:#11151c;--panel2:#151a22;--panel3:#1a202a;--line:#272e3a;--line2:#354052;--ink:#eef2f8;--ink2:#b3bdcb;--muted:#778295;--blue:#79a6ff;--blue2:#4b7df0;--blue-soft:#172747;--green:#4fd6a0;--amber:#f1b84c;--rose:#ff7185;--violet:#af8cff;--cyan:#55d5e8;--shadow:0 18px 55px #0008;--radius:12px;--console-h:clamp(185px,32vh,300px)}
*{box-sizing:border-box}html,body{height:100%;margin:0;overflow:hidden;background:var(--page);color:var(--ink);font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}button,input,textarea,select{font:inherit;color:inherit}button{cursor:pointer}.app{height:100%;display:grid;grid-template-rows:56px minmax(0,1fr) var(--console-h)}
.topbar{display:flex;align-items:center;gap:18px;padding:0 18px;border-bottom:1px solid var(--line);background:#0e1218;z-index:20}.brand{display:flex;align-items:center;gap:10px;font-weight:800;letter-spacing:-.03em}.brand-mark{width:30px;height:30px;border-radius:9px;background:var(--ink);color:#0c0f14;display:grid;place-items:center;font-size:15px}.topnav{display:flex;gap:3px}.topnav a{padding:7px 10px;border-radius:7px;color:var(--ink2);text-decoration:none;font-size:12px}.topnav a:hover{background:var(--panel3);color:var(--ink)}.topnav a[aria-current=page]{background:var(--blue-soft);color:var(--blue);font-weight:700}.top-spacer{flex:1}.lock-pill,.sync-pill{display:flex;align-items:center;gap:7px;color:var(--muted);font-size:10px}.lock-pill{padding:6px 9px;border:1px solid #264638;border-radius:999px;background:#112019;color:#75cda9}.lock-dot,.sync-dot{width:7px;height:7px;border-radius:50%;background:var(--green);box-shadow:0 0 0 4px #4fd6a01b}.top-btn,.icon-btn,.quiet-btn,.primary-btn,.danger-btn{border:1px solid var(--line2);border-radius:8px;background:var(--panel2);padding:7px 10px;font-size:11px}.top-btn:hover,.icon-btn:hover,.quiet-btn:hover{border-color:#5873a5;background:var(--panel3)}.primary-btn{border-color:var(--blue2);background:var(--blue2);color:white;font-weight:700}.primary-btn:hover{filter:brightness(1.1)}.danger-btn{border-color:#713444;background:#321923;color:#ff9bad}.quiet-btn{background:transparent}.icon-btn{width:31px;height:31px;padding:0;display:grid;place-items:center}
.body{min-height:0;display:grid;grid-template-columns:248px minmax(0,1fr);background:var(--page)}.rail{min-height:0;border-right:1px solid var(--line);background:#0f1319;display:flex;flex-direction:column}.rail-head{padding:14px 14px 10px;display:flex;align-items:center;justify-content:space-between}.eyebrow{font-size:9px;font-weight:800;letter-spacing:.13em;text-transform:uppercase;color:var(--muted)}.rail-section{min-height:0;display:flex;flex-direction:column}.rail-section.endeavors{max-height:42%;border-bottom:1px solid var(--line)}.rail-title{display:flex;align-items:center;justify-content:space-between;padding:6px 14px;color:var(--ink2);font-size:11px;font-weight:700}.rail-list{overflow:auto;padding:3px 8px 10px}.rail-card{width:100%;display:grid;gap:3px;padding:9px 10px;border:1px solid transparent;border-radius:8px;background:transparent;text-align:left}.rail-card:hover{background:var(--panel2)}.rail-card.active{background:var(--blue-soft);border-color:#355387}.rail-card strong{font-size:11px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.rail-card span{color:var(--muted);font-size:9px}.rail-card .status-line{display:flex;align-items:center;gap:5px}.status-dot{width:6px;height:6px;border-radius:50%;background:var(--muted)}.status-dot.active,.status-dot.complete{background:var(--green)}.status-dot.running,.status-dot.queued{background:var(--blue);box-shadow:0 0 0 3px #79a6ff1c}.status-dot.failed,.status-dot.stale{background:var(--rose)}.stores{margin-top:auto;border-top:1px solid var(--line);padding-bottom:8px}.store-chip{display:flex;align-items:center;gap:7px;margin:4px 10px;padding:7px 9px;border:1px solid var(--line);border-radius:8px;color:var(--ink2);font-size:10px}.store-chip i{width:7px;height:7px;border-radius:2px;background:var(--violet)}
.workspace{min-width:0;min-height:0;display:grid;grid-template-rows:50px minmax(0,1fr) auto}.workspace-toolbar{display:flex;align-items:center;gap:12px;padding:0 14px;border-bottom:1px solid var(--line);background:var(--panel)}.crumbs{min-width:0;display:flex;align-items:center;gap:7px}.crumbs strong{font-size:12px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.crumbs span{color:var(--muted);font-size:10px}.relationship-note{margin-left:10px;color:var(--muted);font-size:9px}.toolbar-spacer{flex:1}.zoom-group{display:flex;align-items:center;gap:5px}.zoom-label{width:38px;text-align:center;color:var(--muted);font-size:9px}
.viewport{position:relative;min-width:0;min-height:0;overflow:hidden;background-color:#0d1117;background-image:radial-gradient(circle,#303847 1px,transparent 1px);background-size:22px 22px;touch-action:none;cursor:grab}.viewport.panning{cursor:grabbing}.stage{position:absolute;left:0;top:0;width:1800px;height:3000px;transform-origin:0 0;will-change:transform}.edge-layer{position:absolute;inset:0;overflow:visible;pointer-events:none}.conv-edge{fill:none;stroke:#475267;stroke-width:2}.conv-edge.stale{stroke:var(--rose);stroke-dasharray:6 6}.conv-edge.branch{stroke:var(--violet)}.edge-label{font-size:9px;fill:var(--muted)}
.endeavor-envelope,.conversation-envelope{position:absolute;border-radius:20px;pointer-events:none}.endeavor-envelope{border:1px solid #37445a;background:#11172188;box-shadow:inset 0 1px #ffffff08}.conversation-envelope{border:1px solid #2b3442;background:#0e121988}.envelope-title{position:absolute;left:16px;top:-11px;padding:3px 9px;border:1px solid var(--line2);border-radius:999px;background:var(--page);color:var(--ink2);font-size:9px;font-weight:800;letter-spacing:.08em;text-transform:uppercase}.endeavor-envelope>.envelope-title{color:var(--blue);border-color:#355387}.conversation-envelope>.envelope-title{color:var(--violet);border-color:#50406d}
.message-node{position:absolute;width:420px;min-height:146px;border:1px solid var(--line2);border-radius:14px;background:var(--panel);box-shadow:0 12px 30px #0007;overflow:hidden;cursor:pointer;transition:border-color .15s,box-shadow .15s,opacity .15s}.message-node:hover{border-color:#59667a}.message-node.selected{border-color:var(--blue);box-shadow:0 0 0 3px #79a6ff22,0 18px 38px #0009}.message-node.stale{border-color:#75404c}.message-node.paused,.message-node.awaiting_input{border-color:#8a6632}.message-node.context-node{width:350px;border-style:dashed;background:#15172a}.message-head{display:flex;align-items:center;gap:8px;padding:10px 12px;border-bottom:1px solid var(--line);background:#ffffff02}.role-icon{width:25px;height:25px;border-radius:8px;display:grid;place-items:center;font-size:10px;font-weight:900;background:var(--blue-soft);color:var(--blue)}.message-node.user .role-icon{background:#292039;color:var(--violet)}.message-node.context-node .role-icon{background:#26312b;color:var(--green)}.message-title{min-width:0;flex:1}.message-title strong{display:block;font-size:11px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.message-title span{display:block;color:var(--muted);font-size:8px;margin-top:2px}.state-badge{padding:3px 6px;border-radius:999px;background:#1e2733;color:var(--muted);font-size:8px;font-weight:800;text-transform:uppercase}.state-badge.running{background:#162b50;color:var(--blue)}.state-badge.complete{background:#153126;color:var(--green)}.state-badge.stale,.state-badge.failed{background:#371d25;color:var(--rose)}.state-badge.paused,.state-badge.awaiting_input{background:#392b16;color:var(--amber)}.message-copy{padding:11px 13px;color:var(--ink2);font-size:10.5px;line-height:1.45;display:-webkit-box;-webkit-box-orient:vertical;-webkit-line-clamp:4;overflow:hidden;white-space:pre-wrap}.message-meta{display:flex;gap:10px;padding:0 13px 10px;color:var(--muted);font-size:8px}.workflow-shell{margin:0 10px 10px;border:1px solid #2c3a50;border-radius:10px;background:#0b111a;overflow:hidden}.workflow-shell.expanded-source{visibility:hidden}.workflow-head{display:flex;align-items:center;gap:7px;padding:7px 8px;border-bottom:1px solid #263044;color:var(--ink2);font-size:8px}.workflow-head strong{font-size:9px}.workflow-head .active-workflow{margin-left:auto;color:var(--blue);max-width:140px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.workflow-expand{flex:none;border:1px solid #3b5072;border-radius:6px;background:#17243a;padding:3px 6px;color:#a9c5ff;font-size:7px;font-weight:800}.workflow-expand:hover{border-color:var(--blue);background:#1b3157;color:white}.workflow-mini{position:relative;height:94px;overflow:hidden}.wf-line{position:absolute;height:1px;background:#3d4a60;transform-origin:0 50%;pointer-events:none}.wf-node{position:absolute;width:72px;height:34px;padding:4px;border:1px solid #374258;border-radius:6px;background:#161d27;color:var(--muted);font-size:7px;overflow:hidden;text-overflow:ellipsis;display:grid;place-items:center;text-align:center;z-index:2}.wf-node:hover{border-color:var(--blue);color:var(--ink)}.wf-node.active{border-color:var(--blue);background:#18305d;color:#cbdcff;box-shadow:0 0 12px #4b7df066}.wf-node.complete{border-color:#285843;color:#70d7ae}.wf-node.awaiting_input{border-color:#765728;color:var(--amber)}.wf-more{position:absolute;right:7px;bottom:5px;color:var(--muted);font-size:7px}
.workflow-overlays{position:absolute;inset:0;z-index:30;pointer-events:none}.workflow-overlay{position:absolute;display:flex;max-height:calc(100% - 32px);flex-direction:column;overflow:hidden;border:1px solid #53627a;border-radius:16px;background:#10151df8;box-shadow:0 28px 80px #000d,0 0 0 1px #ffffff08;opacity:0;pointer-events:auto;transform-origin:0 0;transition:transform .3s cubic-bezier(.2,.85,.25,1),opacity .2s ease;backdrop-filter:blur(16px);cursor:default}.workflow-overlay.open{opacity:1;transform:none!important}.workflow-overlay.closing{pointer-events:none}.workflow-overlay:focus-within{border-color:#7997c8}.workflow-overlay-head{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:10px;padding:13px 14px 11px;border-bottom:1px solid var(--line2);background:linear-gradient(135deg,#182235,#111821)}.workflow-overlay-title{min-width:0}.workflow-overlay-title .eyebrow{color:#9bbcff}.workflow-overlay-title h2{margin:4px 0 2px;font-size:15px;line-height:1.25;letter-spacing:-.02em}.workflow-overlay-title p{margin:0;color:var(--muted);font-size:9px}.workflow-overlay-controls{display:flex;align-items:flex-start;gap:6px}.overlay-pin[aria-pressed=true]{border-color:#5c88cf;background:#1c3764;color:#c8dbff}.workflow-switcher{grid-column:1/-1;display:flex;gap:5px;overflow:auto;padding-top:2px}.workflow-switcher button{flex:none;border:1px solid var(--line2);border-radius:999px;background:#111722;padding:4px 8px;color:var(--muted);font-size:8px}.workflow-switcher button:hover,.workflow-switcher button.current{border-color:#617eaf;background:#1a2b49;color:#c6d9ff}.workflow-overlay-scroll{min-height:0;overflow:auto;padding:12px}.workflow-card-grid{display:grid;grid-template-columns:repeat(var(--workflow-cols),minmax(0,1fr));align-items:start;gap:10px}.workflow-detail-card{min-width:0;padding:11px;border:1px solid #313b4b;border-radius:11px;background:#131a23;color:var(--ink2);outline:none;transition:border-color .14s,background .14s,transform .14s}.workflow-detail-card:hover,.workflow-detail-card:focus{border-color:#6387c2;background:#172131;transform:translateY(-1px)}.workflow-detail-card.active{border-color:var(--blue);background:#172d55;box-shadow:0 0 0 2px #79a6ff18}.workflow-detail-card.complete{border-color:#2d5c49}.workflow-detail-card.awaiting_input,.workflow-detail-card.awaiting_approval{border-color:#7c5e2d}.workflow-card-top{display:flex;align-items:flex-start;gap:7px}.workflow-card-kind{flex:none;padding:3px 5px;border-radius:5px;background:#202a38;color:#9eb6dc;font-size:7px;font-weight:900;letter-spacing:.05em;text-transform:uppercase}.workflow-card-top h3{min-width:0;margin:1px 0 0;font-size:11px;line-height:1.35;color:var(--ink);overflow-wrap:anywhere}.workflow-card-copy{margin:9px 0 0;color:#c3ccd9;font-size:9px;line-height:1.5;white-space:pre-wrap;overflow-wrap:anywhere}.workflow-card-meta{display:grid;gap:3px;margin-top:9px;padding-top:8px;border-top:1px solid #273142;color:var(--muted);font-size:8px;line-height:1.4;overflow-wrap:anywhere}.workflow-card-section{margin-top:9px}.workflow-card-section strong{display:block;margin-bottom:4px;color:#8e9aae;font-size:7px;letter-spacing:.08em;text-transform:uppercase}.workflow-card-section pre{max-height:none;margin:0;padding:7px;border:1px solid #293344;border-radius:7px;background:#0b0f15;color:#b8c8df;font-size:7.5px;line-height:1.45;white-space:pre-wrap;overflow-wrap:anywhere}.workflow-routes{display:grid;gap:4px}.workflow-route{padding:5px 6px;border-left:2px solid #4d6b9a;background:#101722;color:#aebbd0;font-size:8px;line-height:1.4;overflow-wrap:anywhere}.workflow-empty{padding:20px;color:var(--muted);font-size:10px;text-align:center}
.workflow-overlay-head[data-workflow-drag]{cursor:grab;user-select:none;touch-action:none}.workflow-overlay.dragging .workflow-overlay-head{cursor:grabbing}.workflow-overlay-title .eyebrow{display:flex;align-items:center;gap:8px}.workflow-move-hint{padding:2px 5px;border:1px solid #486087;border-radius:999px;background:#111a29;color:#b7cced;font-size:7px;letter-spacing:.04em}.workflow-overlay-scroll{padding-bottom:24px}.workflow-overlay-resize{position:absolute;right:2px;bottom:2px;z-index:5;width:30px;height:30px;border:0;border-radius:8px 0 12px 0;background:linear-gradient(135deg,transparent 45%,#263b5e 46%);color:#bcd1f4;font-size:16px;line-height:1;cursor:nwse-resize;touch-action:none}.workflow-overlay-resize:hover,.workflow-overlay-resize:focus{background:linear-gradient(135deg,transparent 38%,#31558c 39%);color:white;outline:none}
.composer{padding:8px 14px 10px;border-top:1px solid var(--line);background:var(--panel)}.composer.sending{opacity:.72;pointer-events:none}.composer-card{overflow:hidden;border:1px solid var(--line2);border-radius:13px;background:#0d1117;box-shadow:0 10px 28px #0005}.composer-card:focus-within{border-color:#5478b6;box-shadow:0 0 0 2px #79a6ff14,0 10px 28px #0006}.composer-toolbar{display:flex;align-items:center;gap:4px;padding:5px 8px;border-bottom:1px solid #202936;background:#10161f}.composer-toolbar-label{display:flex;align-items:center;gap:6px;margin-right:3px;color:#91a0b6;font-family:ui-monospace,SFMono-Regular,Menlo,monospace;font-size:8px;font-weight:800}.composer-toolbar-label i{width:6px;height:6px;border-radius:50%;background:var(--green);box-shadow:0 0 7px #4fd6a077}.composer-toolbar button{min-width:26px;height:23px;border:1px solid var(--line);border-radius:5px;background:#131b26;color:var(--ink2);font-size:8px;font-weight:800}.composer-toolbar button:hover{border-color:#5f7ca9;color:white}.markdown-state{margin-left:auto;color:#657085;font-size:8px}.composer-body{display:grid;grid-template-columns:minmax(0,1fr) minmax(260px,.55fr);min-height:66px}.composer-terminal{position:relative;min-width:0;background:#090c11;padding:9px 12px 9px 32px}.composer-terminal:before{content:"❯";position:absolute;left:13px;top:12px;color:var(--green);font-family:ui-monospace,SFMono-Regular,Menlo,monospace;font-size:11px}.composer-terminal textarea{position:relative;z-index:1;width:100%;min-height:48px;max-height:150px;resize:vertical;border:0;background:transparent;caret-color:transparent;color:#c9d6e8;font-family:ui-monospace,SFMono-Regular,Menlo,monospace;font-size:11px;line-height:1.5;outline:none}.composer-preview-shell{min-width:0;max-height:150px;overflow:auto;border-left:1px solid #202936;background:#0d1219;padding:8px 11px}.composer-preview-label{margin-bottom:5px;color:#68758a;font-size:7px;font-weight:900;letter-spacing:.11em;text-transform:uppercase}.composer-preview{color:#dce4ef;font-size:10px;line-height:1.48;overflow-wrap:anywhere}.composer-preview p{margin:0 0 6px}.composer-preview p:last-child,.composer-preview ul:last-child,.composer-preview pre:last-child{margin-bottom:0}.composer-preview ul{margin:0 0 6px;padding-left:18px}.composer-preview h1,.composer-preview h2,.composer-preview h3{margin:0 0 6px;font-size:11px}.composer-preview code{padding:1px 3px;border-radius:3px;background:#1c2532;color:#c5d8f3;font-family:ui-monospace,SFMono-Regular,Menlo,monospace}.composer-preview pre{margin:0 0 6px;padding:6px;border:1px solid var(--line);border-radius:6px;background:#080b0f;white-space:pre-wrap}.preview-empty{color:#5e697a}.composer-foot{display:flex;align-items:center;gap:9px;padding:5px 7px 6px 11px;border-top:1px solid #202936;background:#0e141c}.composer-foot .branch-note{color:var(--muted);font-size:8px}.composer-hint{margin-left:auto;color:#657085;font-size:8px}.send-btn{height:30px;min-width:82px}.terminal-block-cursor{position:absolute;z-index:2;display:none;pointer-events:none;border:1px solid #d8e5f7;background:#a9bedcaa;box-shadow:0 0 7px #9eb9e477;animation:terminal-cursor-blink 1.05s steps(1,end) infinite}@keyframes terminal-cursor-blink{0%,52%{opacity:1}53%,100%{opacity:0}}.execution-summary{display:grid;grid-template-columns:110px minmax(0,1fr);gap:7px 10px;margin:0}.execution-summary dt{color:var(--muted);font-size:9px}.execution-summary dd{margin:0;color:var(--ink);font-family:ui-monospace,SFMono-Regular,Menlo,monospace;font-size:9px;overflow-wrap:anywhere}.boundary-note{margin:3px 0 0;padding:10px;border:1px solid #334565;border-radius:8px;background:#111b2a;color:#b8c7dc;font-size:9px;line-height:1.5}
.console{position:relative;z-index:15;border-top:1px solid #3a4659;background:#0b0e13;box-shadow:0 -18px 45px #0007;min-height:0;display:grid;grid-template-rows:34px minmax(0,1fr)}.console-handle{position:absolute;top:-4px;left:50%;width:64px;height:5px;border-radius:9px;background:#465268;transform:translateX(-50%);cursor:ns-resize}.console-bar{display:flex;align-items:center;gap:9px;padding:0 14px;border-bottom:1px solid var(--line);background:#10141b}.terminal-dots{display:flex;gap:5px}.terminal-dots i{width:7px;height:7px;border-radius:50%;background:#394252}.terminal-dots i:first-child{background:var(--rose)}.terminal-dots i:nth-child(2){background:var(--amber)}.terminal-dots i:nth-child(3){background:var(--green)}.console-title{font-size:10px;font-weight:800}.console-subtitle{color:var(--muted);font-size:9px}.console-actions{margin-left:auto;display:flex;gap:6px}.console-body{min-height:0;display:grid;grid-template-columns:minmax(260px,.8fr) minmax(520px,1.4fr);gap:1px;background:var(--line)}.render-panel,.edit-panel{min-height:0;background:#0d1117;overflow:auto;padding:12px 14px}.panel-label{display:flex;align-items:center;justify-content:space-between;margin-bottom:8px;color:var(--muted);font-size:8px;font-weight:800;letter-spacing:.12em;text-transform:uppercase}.rendered{color:#d6dde8;font-size:11px;line-height:1.55;white-space:pre-wrap;word-break:break-word}.rendered p{margin:0 0 10px}.rendered pre{padding:10px;border:1px solid var(--line);border-radius:8px;background:#080b0f;overflow:auto;color:#bcd0ef}.empty-selection{height:100%;display:grid;place-items:center;color:var(--muted);font-size:11px;text-align:center}.field-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px}.editor-field{display:grid;gap:5px;min-width:0}.editor-field>span{display:flex;align-items:center;justify-content:space-between;color:var(--ink2);font-size:9px;font-weight:800}.editor-field small{font-weight:400;color:var(--muted)}.editor-field textarea,.editor-field input,.editor-field select{width:100%;border:1px solid var(--line2);border-radius:8px;background:#090c11;padding:8px;outline:none;font-family:ui-monospace,SFMono-Regular,Menlo,monospace;font-size:10px;line-height:1.45}.editor-field textarea{height:135px;resize:vertical}.editor-field textarea:focus,.editor-field input:focus,.editor-field select:focus{border-color:var(--blue)}.workflow-editor{display:grid;grid-template-columns:1fr 1.2fr;gap:10px}.workflow-editor .editor-field textarea{height:100px}.editor-footer{display:flex;align-items:center;gap:8px;margin-top:9px}.apply-global{display:flex;align-items:center;gap:6px;margin-left:auto;color:var(--ink2);font-size:9px}.apply-global input{accent-color:var(--blue)}
.modal{width:min(520px,calc(100vw - 30px));border:1px solid var(--line2);border-radius:14px;background:var(--panel);color:var(--ink);box-shadow:var(--shadow);padding:0}.modal::backdrop{background:#05070ad9;backdrop-filter:blur(4px)}.modal-head{display:flex;align-items:center;justify-content:space-between;padding:13px 15px;border-bottom:1px solid var(--line)}.modal-head strong{font-size:13px}.modal-body{display:grid;gap:11px;padding:15px}.modal-body label{display:grid;gap:5px;color:var(--ink2);font-size:9px;font-weight:700}.modal-body input,.modal-body textarea,.modal-body select{border:1px solid var(--line2);border-radius:8px;background:#0b0f15;padding:8px}.modal-body textarea{min-height:90px;resize:vertical}.modal-actions{display:flex;justify-content:flex-end;gap:7px;padding:11px 15px;border-top:1px solid var(--line)}.toast{position:fixed;right:18px;bottom:calc(var(--console-h) + 18px);max-width:380px;padding:10px 13px;border:1px solid var(--line2);border-radius:9px;background:#171d27;box-shadow:var(--shadow);font-size:10px;z-index:40;opacity:0;transform:translateY(7px);pointer-events:none;transition:.18s}.toast.show{opacity:1;transform:none}.toast.error{border-color:#75404c;color:#ffabb7}
@media(max-width:900px){:root{--console-h:clamp(230px,42vh,350px)}.body{grid-template-columns:190px minmax(0,1fr)}.relationship-note,.lock-pill{display:none}.composer-body{grid-template-columns:1fr}.composer-preview-shell{display:none}.console-body{grid-template-columns:1fr}.render-panel{display:none}.field-grid,.workflow-editor{grid-template-columns:1fr}.message-node{width:360px}.topnav a:nth-last-child(-n+2){display:none}}
@media(max-width:620px){:root{--console-h:clamp(260px,42vh,360px)}.app{grid-template-rows:52px minmax(0,1fr) var(--console-h)}.body{grid-template-columns:1fr}.rail{display:none}.topbar{padding:0 10px;gap:8px}.brand span:last-child{display:none}.topnav a{padding:6px}.sync-pill{display:none}.workspace{grid-template-rows:46px minmax(0,1fr) auto}.workspace-toolbar{padding-inline:9px}.toolbar-spacer+.quiet-btn{display:none}.composer{padding:6px}.composer-hint{display:none}.console-subtitle{display:none}.console-actions .quiet-btn{display:inline-flex;padding:5px 7px;font-size:9px}.edit-panel{padding:9px}.editor-field textarea{height:112px}}
@media(prefers-reduced-motion:reduce){*{scroll-behavior:auto!important;transition:none!important}.terminal-block-cursor{animation:none!important}}
.status-dot.needs_attention{background:var(--rose)}
.state-badge.needs_attention{background:#371d25;color:var(--rose)}
.message-node.failed,.message-node.needs_attention{border-color:#75404c}
.app.console-empty{grid-template-rows:56px minmax(0,1fr) 116px}
@media(max-width:620px){.app.console-empty{grid-template-rows:52px minmax(0,1fr) 116px}}
.rail-section.endeavors{flex:1;max-height:none;border-bottom:0}.rail-help{margin:0 14px 8px;color:var(--muted);font-size:9px;line-height:1.45}.rail-group{margin-bottom:5px;border:1px solid transparent;border-radius:9px}.rail-group.active{border-color:#263b61;background:#121b2a}.rail-group .rail-card{border-radius:8px 8px 4px 4px}.conversation-children{position:relative;margin:0 7px 6px 18px;padding-left:10px;border-left:1px solid #344159}.rail-conversation{position:relative;width:100%;display:grid;gap:2px;padding:7px 8px;border:0;border-radius:6px;background:transparent;text-align:left}.rail-conversation:before{content:"";position:absolute;left:-11px;top:15px;width:8px;border-top:1px solid #344159}.rail-conversation:hover{background:var(--panel3)}.rail-conversation.active{background:#24334e;color:white}.rail-conversation strong{font-size:10px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.rail-conversation span{color:var(--muted);font-size:8px}.unassigned-section{max-height:28%;border-top:1px solid var(--line)}.unassigned-section[hidden]{display:none}.unassigned-section .rail-title{color:var(--amber)}
.approval-inbox{border-color:#775a2c;background:#2b2113;color:#ffd483}.approval-inbox[hidden]{display:none}.message-node.awaiting_approval{border-color:var(--amber);box-shadow:0 0 0 2px #f1b84c1c,0 12px 30px #0007}.state-badge.awaiting_approval{background:#3b2b13;color:#ffd27b}.approval-flag{margin-left:auto;color:#ffd27b;font-size:8px;font-weight:800;text-transform:uppercase}.wf-node.awaiting_approval{border-color:var(--amber);background:#342713;color:#ffd27b}.approval-panel{margin-bottom:12px;padding:12px;border:1px solid #765827;border-radius:10px;background:#211a10}.approval-panel header{display:flex;align-items:center;gap:8px;margin-bottom:8px}.approval-panel header strong{font-size:11px}.approval-panel header span{margin-left:auto;padding:3px 6px;border-radius:999px;background:#493519;color:#ffd27b;font-size:8px;font-weight:800;text-transform:uppercase}.approval-panel p{margin:0 0 8px;color:#e8d7b5;font-size:10px;line-height:1.45}.approval-targets{padding:7px 8px;border:1px solid #5d4726;border-radius:7px;background:#100d08;color:#ffd27b;font-family:ui-monospace,SFMono-Regular,Menlo,monospace;font-size:9px;word-break:break-all}.approval-panel details{margin:8px 0;color:var(--ink2);font-size:9px}.approval-panel pre{max-height:150px;margin:6px 0;padding:8px;overflow:auto;border:1px solid var(--line);border-radius:7px;background:#090b0e;color:#cad4e2;font-size:8px;white-space:pre-wrap}.approval-panel textarea{width:100%;min-height:54px;resize:vertical;border:1px solid #665033;border-radius:7px;background:#0b0c0e;padding:7px;font-size:9px}.approval-decisions{display:flex;justify-content:flex-end;gap:7px;margin-top:7px}
.status-dot.awaiting_approval{background:var(--amber);box-shadow:0 0 0 3px #f1b84c1c}
.rail-title-actions{display:flex;align-items:center;gap:4px}.rail-create{height:24px;padding:0 6px;border:1px solid var(--line2);border-radius:7px;background:var(--panel2);color:var(--ink2);font-size:8px;font-weight:900}.rail-create:hover{border-color:#5873a5;color:var(--ink)}
.conversation-name{display:flex;min-width:0;align-items:center;gap:5px;border:0;background:transparent;padding:4px 6px;border-radius:6px;color:var(--ink);font-size:12px;font-weight:800}.conversation-name:hover,.conversation-name:focus-visible{background:var(--panel3);outline:none}.conversation-name span{max-width:260px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;color:inherit;font-size:inherit}.conversation-name i{color:var(--muted);font-size:9px;font-style:normal}.conversation-rename{display:flex;align-items:center;gap:5px}.conversation-rename[hidden]{display:none}.conversation-rename input{width:min(280px,32vw);border:1px solid var(--blue2);border-radius:6px;background:#090d13;padding:5px 7px;font-size:11px;font-weight:700;outline:none}.conversation-rename button{height:27px;padding:0 7px}
.conversation-welcome{position:absolute;left:50%;top:45%;z-index:12;width:min(560px,calc(100% - 48px));transform:translate(-50%,-50%);text-align:center;pointer-events:none}.conversation-welcome[hidden]{display:none}.conversation-welcome h2{margin:7px 0 6px;font-size:24px;letter-spacing:-.04em}.conversation-welcome p{margin:0 auto;max-width:470px;color:var(--muted);font-size:10px;line-height:1.55}
.workflow-shell{border-color:#395579;background:#09111c}.workflow-head{min-height:35px;background:linear-gradient(90deg,#101d31,#0b1421)}.workflow-head .workflow-identity{display:grid;gap:1px}.workflow-head .workflow-identity b{color:#c9dcff;font-size:8px;letter-spacing:.06em}.workflow-head .workflow-identity small{color:#71829c;font-size:7px}.workflow-run-stats{margin-left:auto;display:flex;gap:5px}.workflow-run-stats span{padding:3px 5px;border:1px solid #314663;border-radius:999px;background:#101b2c;color:#9eb8dc;font-size:7px;white-space:nowrap}.workflow-run-mini{height:132px;overflow:hidden;background-color:#090e16;background-image:radial-gradient(circle,#28364a 1px,transparent 1px);background-size:14px 14px}.workflow-graph{display:block;min-width:100%;height:100%}.workflow-graph .wg-edge{fill:none;stroke:#3a475b;stroke-width:2}.workflow-graph .wg-edge.loop{stroke-dasharray:6 5;stroke:#5d4d77}.workflow-graph .wg-edge.observed{stroke:#62a1ff;stroke-width:3;filter:drop-shadow(0 0 3px #4b8cff88)}.workflow-graph .wg-edge.active{stroke:#9bc4ff;stroke-width:3}.workflow-graph .wg-edge-label{fill:#7890ae;font-size:7px;font-weight:700}.workflow-graph .wg-node{cursor:pointer;outline:none}.workflow-graph .wg-node rect{fill:#121a26;stroke:#3b485c;stroke-width:1.4}.workflow-graph .wg-node .wg-accent{fill:#617089}.workflow-graph .wg-node text{pointer-events:none}.workflow-graph .wg-node .wg-kind{fill:#8290a5;font-size:8px;font-weight:800;text-transform:uppercase}.workflow-graph .wg-node .wg-label{fill:#d5deea;font-size:10px;font-weight:800}.workflow-graph .wg-node .wg-count{fill:#7f8ca0;font-size:8px}.workflow-graph .wg-node.observed rect{fill:#10273e;stroke:#5498f8}.workflow-graph .wg-node.observed .wg-accent{fill:#559cff}.workflow-graph .wg-node.current rect{fill:#18345f;stroke:#a4c8ff;stroke-width:2;filter:drop-shadow(0 0 8px #4b8cff88)}.workflow-graph .wg-node.waiting rect{fill:#3a2b13;stroke:#f1b84c}.workflow-graph .wg-node.failed rect{fill:#371923;stroke:#ff7185}.workflow-graph .wg-node.selected rect{stroke:#fff;stroke-width:2.4}.workflow-overlay{border:2px solid #8db8ff;background:#0d1420fb;box-shadow:0 32px 100px #000e,0 0 0 1px #d6e6ff55,0 0 34px #4b8cff35}.workflow-overlay-head{background:linear-gradient(135deg,#20375f,#111a29)}.workflow-overlay-title .eyebrow{color:#d4e4ff}.workflow-overlay-scroll{flex:1;padding:0;overflow:hidden}.workflow-run-layout{display:grid;grid-template-columns:minmax(500px,1.45fr) minmax(330px,.8fr);min-height:360px;height:100%}.workflow-run-canvas{min-width:0;min-height:0;overflow:auto;background-color:#080d14;background-image:radial-gradient(circle,#2c3d55 1px,transparent 1px);background-size:18px 18px;cursor:grab;touch-action:none}.workflow-run-canvas.panning{cursor:grabbing}.workflow-run-canvas .workflow-graph{min-width:900px;height:auto;min-height:100%}.workflow-run-inspector{min-width:0;overflow:auto;border-left:1px solid #456187;background:#111925;padding:14px}.workflow-run-inspector h3{margin:5px 0 4px;font-size:15px;letter-spacing:-.02em}.workflow-run-inspector .stage-description{margin:0 0 10px;color:#aebbd0;font-size:9px;line-height:1.5}.workflow-run-overview{display:flex;flex-wrap:wrap;gap:6px;margin:0 0 11px}.workflow-run-overview span{padding:4px 7px;border:1px solid #3e5d89;border-radius:999px;background:#16243a;color:#bfd5f7;font-size:8px}.stage-meta{display:grid;grid-template-columns:84px minmax(0,1fr);gap:5px 8px;margin:0 0 12px;font-size:8.5px}.stage-meta dt{color:#718096}.stage-meta dd{margin:0;color:#cbd5e4;overflow-wrap:anywhere}.stage-section{margin-top:10px}.stage-section>strong{display:block;margin-bottom:5px;color:#8da2bf;font-size:7px;letter-spacing:.1em;text-transform:uppercase}.stage-section pre,.model-step pre{max-height:210px;margin:0;padding:8px;overflow:auto;border:1px solid #2c3d56;border-radius:7px;background:#080c12;color:#c1d2e9;font:8px/1.5 ui-monospace,SFMono-Regular,Menlo,monospace;white-space:pre-wrap;overflow-wrap:anywhere}.model-step{margin-top:7px;border:1px solid #314764;border-radius:8px;background:#0d1420}.model-step summary{padding:8px;color:#d0def1;font-size:8px;font-weight:800;cursor:pointer}.model-step-grid{display:grid;grid-template-columns:1fr 1fr;gap:7px;padding:0 7px 7px}.model-step-grid label{display:grid;gap:4px;color:#70819a;font-size:7px;font-weight:800;text-transform:uppercase}.model-step-grid pre{text-transform:none}.runtime-event{margin-top:5px;padding:7px 8px;border-left:2px solid #4f87d2;background:#0d1522;color:#aebed4;font-size:8px;line-height:1.45}.workflow-loading{display:grid;min-height:280px;place-items:center;color:#9ab6df;font-size:10px}.console-body.message-editor-mode{grid-template-columns:1fr}.node-editor-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px}.node-editor-grid .composer-card{min-width:0;box-shadow:none}.node-editor-grid .composer-body{grid-template-columns:minmax(0,1fr) minmax(180px,.65fr)}.node-editor-grid .composer-terminal textarea{min-height:92px;max-height:220px}.node-editor-grid .composer-preview-shell{max-height:220px}.message-edit-panel{display:grid;align-content:start;gap:9px}.message-edit-panel .editor-footer{margin-top:0}@media(max-width:1100px){.workflow-run-layout{grid-template-columns:1fr}.workflow-run-inspector{max-height:280px;border-left:0;border-top:1px solid #456187}.node-editor-grid{grid-template-columns:1fr}.node-editor-grid .composer-body{grid-template-columns:1fr}.node-editor-grid .composer-preview-shell{display:none}}@media(max-width:720px){.workflow-run-layout{min-height:320px}.workflow-run-canvas .workflow-graph{min-width:720px}}
</style>
</head>
<body>
<div class="app">
  <header class="topbar">
    <div class="brand"><span class="brand-mark">⌁</span><span>llm-super</span></div>
    <nav class="topnav" aria-label="Primary">
      <a href="/workspace" aria-current="page">Workspace</a><a href="/">Live</a><a href="/history">History</a><a href="/graphs">Agent Graphs</a>
    </nav>
    <div class="top-spacer"></div>
    <button class="top-btn approval-inbox" id="approvalInbox" hidden>Approval required</button>
    <button class="lock-pill" id="backendLockButton" type="button" title="Inspect the operator-owned execution boundary"><i class="lock-dot"></i><span id="backendLock">Execution locked</span></button>
    <div class="sync-pill"><i class="sync-dot"></i><span id="syncState">Connecting</span></div>
    <button class="top-btn" id="newEndeavor" type="button">New endeavor</button>
  </header>
  <div class="body">
    <aside class="rail" aria-label="Workspace navigation">
      <div class="rail-head"><span class="eyebrow">Workspaces</span><button class="icon-btn" id="railRefresh" title="Refresh">↻</button></div>
      <section class="rail-section endeavors"><div class="rail-title"><span>Endeavors</span><span class="rail-title-actions"><button class="rail-create" id="addConversation" type="button" title="New conversation in selected endeavor">Chat +</button><button class="rail-create" id="addEndeavor" type="button" title="New endeavor">Endeavor +</button></span></div><p class="rail-help">An endeavor is a larger objective. Its conversations are the indented chat threads beneath it.</p><div class="rail-list" id="endeavorList"></div></section>
      <section class="rail-section unassigned-section" id="unassignedSection" hidden><div class="rail-title"><span>Not in an endeavor</span></div><p class="rail-help">Archived conversations are listed here until opened; they will be grouped under “Unassigned conversations.”</p><div class="rail-list" id="unassignedList"></div></section>
      <section class="stores"><div class="rail-title"><span>Knowledge stores</span><button class="icon-btn" id="addStore" title="New knowledge store">+</button></div><div id="storeList"></div></section>
    </aside>
    <main class="workspace">
      <div class="workspace-toolbar">
        <div class="crumbs"><button class="conversation-name" id="endeavorTitle" title="Rename endeavor"><span id="endeavorTitleText">Choose an endeavor</span><i>✎</i></button><form class="conversation-rename" id="endeavorRename" hidden><input id="endeavorRenameInput" maxlength="200" aria-label="Endeavor title"><button class="primary-btn" type="submit">Save</button><button class="quiet-btn" type="button" id="cancelEndeavorRename">Cancel</button></form><span>contains</span><button class="conversation-name" id="conversationTitle" title="Rename conversation"><span id="conversationTitleText">conversation</span><i>✎</i></button><form class="conversation-rename" id="conversationRename" hidden><input id="conversationRenameInput" maxlength="200" aria-label="Conversation title"><button class="primary-btn" type="submit">Save</button><button class="quiet-btn" type="button" id="cancelConversationRename">Cancel</button></form></div>
        <span class="relationship-note">Conversation messages flow downward · each assistant message contains its workflow instance</span>
        <div class="toolbar-spacer"></div>
        <button class="quiet-btn" id="addContext">+ Context node</button>
        <div class="zoom-group"><button class="icon-btn" id="zoomOut">−</button><span class="zoom-label" id="zoomLabel">80%</span><button class="icon-btn" id="zoomIn">+</button><button class="quiet-btn" id="fitGraph">Fit</button></div>
      </div>
      <div class="viewport" id="viewport" aria-label="Downward-growing conversation graph. Drag the background to pan; click a message or nested workflow node to inspect and edit it.">
        <div class="stage" id="stage"><svg class="edge-layer" id="edgeLayer" width="1800" height="3000" aria-hidden="true"></svg><div id="envelopes"></div><div id="nodes"></div></div>
        <div class="workflow-overlays" id="workflowOverlays" aria-live="polite"></div>
        <section class="conversation-welcome" id="conversationWelcome" aria-label="Start a new conversation" hidden>
          <span class="eyebrow" id="welcomeEyebrow">New conversation</span><h2 id="welcomeTitle">What would you like to work on?</h2><p id="welcomeCopy">Use the composer below. An LLM will name this conversation after the first response, and you can rename it at any time.</p>
        </section>
      </div>
      <form class="composer" id="composer">
        <div class="composer-card" data-markdown-editor>
          <div class="composer-toolbar" aria-label="Markdown formatting"><span class="composer-toolbar-label"><i></i>prompt.md</span><button type="button" data-markdown-command="bold" title="Bold · inserts Markdown">B</button><button type="button" data-markdown-command="italic" title="Italic · inserts Markdown"><em>I</em></button><button type="button" data-markdown-command="list" title="Bulleted list · inserts Markdown">• List</button><button type="button" data-markdown-command="code" title="Code block · inserts Markdown">&lt;/&gt;</button><span class="markdown-state">Markdown source · live rendered output</span></div>
          <div class="composer-body"><div class="composer-terminal" id="messageTerminal"><textarea id="messageTerminalInput" aria-label="Message in Markdown" spellcheck="true" placeholder="Ask, direct, or continue this conversation…"></textarea><i class="terminal-block-cursor" id="terminalBlockCursor" aria-hidden="true"></i></div><div class="composer-preview-shell" aria-live="polite"><div class="composer-preview-label">Rendered output</div><div class="composer-preview" id="messagePreview"><span class="preview-empty">Your formatted message will render here.</span></div></div></div>
          <div class="composer-foot"><span class="branch-note" id="branchNote">continues the latest message</span><span class="composer-hint">Markdown supported · ⌘/Ctrl+Enter to send</span><button class="primary-btn send-btn" type="submit">Send ↵</button></div>
        </div>
      </form>
    </main>
  </div>
  <section class="console" aria-label="Selected node editor">
    <div class="console-handle" id="consoleHandle" title="Resize editor"></div>
    <div class="console-bar"><span class="terminal-dots"><i></i><i></i><i></i></span><span class="console-title" id="consoleTitle">Node terminal</span><span class="console-subtitle" id="consoleSubtitle">Select a node to inspect its complete input and output</span><div class="console-actions" id="consoleActions"></div></div>
    <div class="console-body" id="consoleBody"><div class="empty-selection">Select a conversation node.<br>Its full message and editable input/output will appear here.</div></div>
  </section>
</div>
<dialog class="modal" id="entityModal"><form method="dialog" id="entityForm"><div class="modal-head"><strong id="entityTitle">Create</strong><button class="icon-btn" value="cancel">×</button></div><div class="modal-body" id="entityFields"></div><div class="modal-actions"><button class="quiet-btn" value="cancel">Cancel</button><button class="primary-btn" value="default" id="entitySubmit">Create</button></div></form></dialog>
<dialog class="modal" id="executionModal"><form method="dialog"><div class="modal-head"><strong>Execution boundary</strong><button class="icon-btn" value="close" aria-label="Close execution boundary">×</button></div><div class="modal-body"><dl class="execution-summary" id="executionSummary"></dl><p class="boundary-note">This is an operator-owned security boundary, not an agent preference. Credentials stay in the controller environment. Changing the target requires staged deployment configuration, a read-only validation probe, and controller restart so an agent cannot retarget its own process spawns.</p></div><div class="modal-actions"><button class="primary-btn" value="close">Done</button></div></form></dialog>
<div class="toast" id="toast" role="status" aria-live="polite"></div>
<script>
const $=s=>document.querySelector(s), esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
const state={endeavors:[],unassigned:[],graph:null,models:[],flows:[],agents:[],selected:null,zoom:.8,panX:40,panY:25,panning:null,serial:0,poll:null,modalAction:null,expandedWorkflows:{},workflowExecutions:{},activeWorkflowOverlay:null,overlayZ:30,composerSession:null,executionLock:null,creatingEndeavor:false,creatingConversation:false,sending:false};
async function api(url,opts={}){let response;try{response=await fetch(url,{headers:{"Content-Type":"application/json",...(opts.headers||{})},...opts})}catch(error){$("#syncState").textContent="disconnected";throw new Error("Workspace API is unavailable. This page may be connected to a stopped or expired server; reopen the active llm-super URL and try again.")}let data={};try{data=await response.json()}catch{}if(!response.ok)throw new Error(data.detail||data.error||`Request failed (${response.status})`);return data}
function toast(message,error=false){const el=$("#toast");el.textContent=message;el.className="toast show"+(error?" error":"");clearTimeout(toast.timer);toast.timer=setTimeout(()=>el.className="toast",3200)}
function short(text,n=90){text=String(text||"").replace(/\s+/g," ").trim();return text.length>n?text.slice(0,n-1)+"…":text}
function timeLabel(ts){if(!ts)return "";return new Date(ts*1000).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit"})}
function inlineMarkdown(text){return text.replace(/`([^`\n]+)`/g,"<code>$1</code>").replace(/\*\*([^*\n]+)\*\*/g,"<strong>$1</strong>").replace(/(^|[^*])\*([^*\n]+)\*/g,"$1<em>$2</em>")}
function renderMarkdownBlock(part){const lines=part.split("\n"),out=[],paragraph=[],items=[];const flushParagraph=()=>{if(paragraph.length){out.push("<p>"+paragraph.join("<br>")+"</p>");paragraph.length=0}},flushList=()=>{if(items.length){out.push("<ul>"+items.map(item=>"<li>"+item+"</li>").join("")+"</ul>");items.length=0}};lines.forEach(line=>{if(!line.trim()){flushParagraph();flushList();return}const heading=line.match(/^(#{1,3})\s+(.+)$/),bullet=line.match(/^[-*]\s+(.+)$/);if(heading){flushParagraph();flushList();const level=heading[1].length;out.push("<h"+level+">"+inlineMarkdown(heading[2])+"</h"+level+">")}else if(bullet){flushParagraph();items.push(inlineMarkdown(bullet[1]))}else{flushList();paragraph.push(inlineMarkdown(line))}});flushParagraph();flushList();return out.join("")}
function rich(text){const safe=esc(text||""),parts=safe.split(/```/);return parts.map((part,i)=>i%2?"<pre><code>"+part.replace(/^\w+\n/,"")+"</code></pre>":renderMarkdownBlock(part)).join("")||'<span style="color:var(--muted)">No content yet.</span>'}
function composerPrompt(){return $("#messageTerminalInput").value.trim()}
function markdownEditorMarkup(id,label,value,hint){return`<div class="composer-card" data-markdown-editor><div class="composer-toolbar" aria-label="${esc(label)} Markdown formatting"><span class="composer-toolbar-label"><i></i>${esc(label)}</span><button type="button" data-markdown-command="bold" title="Bold · inserts Markdown">B</button><button type="button" data-markdown-command="italic" title="Italic · inserts Markdown"><em>I</em></button><button type="button" data-markdown-command="list" title="Bulleted list · inserts Markdown">• List</button><button type="button" data-markdown-command="code" title="Code block · inserts Markdown">&lt;/&gt;</button><span class="markdown-state">one Markdown source</span></div><div class="composer-body"><div class="composer-terminal"><textarea id="${esc(id)}" aria-label="${esc(label)} in Markdown" spellcheck="true">${esc(value||"")}</textarea><i class="terminal-block-cursor" aria-hidden="true"></i></div><div class="composer-preview-shell" aria-live="polite"><div class="composer-preview-label">Rendered output</div><div class="composer-preview">${value?rich(value):'<span class="preview-empty">No content yet.</span>'}</div></div></div><div class="composer-foot"><span class="branch-note">${esc(hint)}</span><span class="composer-hint">Markdown source · rich rendered output</span></div></div>`}
function renderMarkdownPreview(root){const input=root.querySelector("textarea"),preview=root.querySelector(".composer-preview"),text=input?.value||"";if(preview)preview.innerHTML=text.trim()?rich(text):'<span class="preview-empty">No content yet.</span>'}
function renderComposerPreview(){const root=$("#composer [data-markdown-editor]");if(root)renderMarkdownPreview(root)}
function applyMarkdown(command,input=$("#messageTerminalInput")){const value=input.value,start=input.selectionStart,end=input.selectionEnd,selected=value.slice(start,end);let from=start,to=end,replacement="",innerStart=null,innerLength=0;if(command==="bold"||command==="italic"){const mark=command==="bold"?"**":"*",content=selected||(command==="bold"?"bold text":"italic text");replacement=mark+content+mark;innerStart=start+mark.length;innerLength=content.length}else if(command==="list"){from=value.lastIndexOf("\n",Math.max(0,start-1))+1;const next=value.indexOf("\n",end);to=next<0?value.length:next;const block=value.slice(from,to)||"list item";replacement=block.split("\n").map(line=>line.startsWith("- ")?line:"- "+line).join("\n")}else{const content=selected||"code";replacement="```\n"+content+"\n```";innerStart=start+4;innerLength=content.length}input.setRangeText(replacement,from,to,"end");if(innerStart!==null)input.setSelectionRange(innerStart,innerStart+innerLength);else input.setSelectionRange(from,from+replacement.length);input.focus();input.dispatchEvent(new Event("input",{bubbles:true}))}
const terminalCursorFrames=new WeakMap();
function scheduleTerminalCursor(input){if(!input)return;cancelAnimationFrame(terminalCursorFrames.get(input)||0);terminalCursorFrames.set(input,requestAnimationFrame(()=>positionTerminalCursor(input)))}
function positionTerminalCursor(input){
  const host=input?.closest(".composer-terminal"),cursor=host?.querySelector(".terminal-block-cursor");
  if(!input||!cursor||!host)return;
  if(document.activeElement!==input){cursor.style.display="none";return}
  const style=getComputedStyle(input),mirror=document.createElement("div"),caret=input.selectionDirection==="backward"?input.selectionStart:input.selectionEnd;
  Object.assign(mirror.style,{position:"fixed",visibility:"hidden",left:"-10000px",top:"0",width:input.clientWidth+"px",boxSizing:"border-box",border:"0",padding:style.padding,whiteSpace:"pre-wrap",overflowWrap:"break-word",fontFamily:style.fontFamily,fontSize:style.fontSize,fontWeight:style.fontWeight,fontStyle:style.fontStyle,lineHeight:style.lineHeight,letterSpacing:style.letterSpacing,tabSize:style.tabSize});
  mirror.append(document.createTextNode(input.value.slice(0,caret)));const marker=document.createElement("span");marker.style.display="inline-block";marker.textContent="M";mirror.append(marker);document.body.append(mirror);
  const markerRect=marker.getBoundingClientRect(),mirrorRect=mirror.getBoundingClientRect(),inputRect=input.getBoundingClientRect(),hostRect=host.getBoundingClientRect(),lineHeight=parseFloat(style.lineHeight)||markerRect.height;
  cursor.style.left=inputRect.left-hostRect.left+markerRect.left-mirrorRect.left-input.scrollLeft+"px";cursor.style.top=inputRect.top-hostRect.top+markerRect.top-mirrorRect.top-input.scrollTop+"px";cursor.style.width=Math.max(7,markerRect.width)+"px";cursor.style.height=lineHeight+"px";cursor.style.display="block";mirror.remove()
}
function wireMarkdownEditor(root){if(!root||root.dataset.markdownWired)return;root.dataset.markdownWired="true";const input=root.querySelector("textarea");root.querySelectorAll("[data-markdown-command]").forEach(button=>button.onmousedown=e=>{e.preventDefault();applyMarkdown(button.dataset.markdownCommand,input)});input.onkeydown=e=>{if((e.metaKey||e.ctrlKey)&&["b","i"].includes(e.key.toLowerCase())){e.preventDefault();applyMarkdown(e.key.toLowerCase()==="b"?"bold":"italic",input)}else if(input.id==="messageTerminalInput"&&e.key==="Enter"&&(e.metaKey||e.ctrlKey)){e.preventDefault();$("#composer").requestSubmit()}};["input","keyup","click","select","focus","keydown"].forEach(type=>input.addEventListener(type,()=>scheduleTerminalCursor(input)));input.addEventListener("input",()=>renderMarkdownPreview(root));input.addEventListener("scroll",()=>positionTerminalCursor(input));input.addEventListener("blur",()=>{const cursor=root.querySelector(".terminal-block-cursor");if(cursor)cursor.style.display="none"});renderMarkdownPreview(root)}
function renderConversationWelcome(){
  const panel=$("#conversationWelcome"),session=state.graph?.conversation?.session,empty=Boolean(session)&&!(state.graph?.nodes||[]).length;
  panel.hidden=!empty;if(!empty)return;
  const newEndeavor=["Untitled endeavor","My first endeavor"].includes(state.graph?.endeavor?.title);
  $("#welcomeEyebrow").textContent=newEndeavor?"New endeavor · first conversation":"New conversation";
  $("#welcomeTitle").textContent=newEndeavor?"What would you like to accomplish?":"What would you like to work on?";
  $("#welcomeCopy").textContent=newEndeavor?"Use the same composer below. An LLM will name this endeavor and its first conversation after the first response; both remain renameable.":"Use the composer below. An LLM will name this conversation after the first response, and you can rename it at any time.";
  if(state.composerSession!==session){state.composerSession=session;$("#messageTerminalInput").value="";renderComposerPreview()}
}
function renderExecutionBoundary(){const lock=state.executionLock||{},target=lock.target||{},rows=[["Backend",lock.backend||"not configured"],["Project",target.project||"active gcloud project"],["Account",target.account||"active gcloud account"],["Zone",target.zone||"—"],["Machine",target.machine_type||"—"],["Provisioning",lock.provisioning||"—"],["Lifecycle",lock.lifecycle||"—"],["Configuration",lock.configuration_source||"models.yaml"]];$("#executionSummary").innerHTML=rows.map(([key,value])=>`<dt>${esc(key)}</dt><dd>${esc(value)}</dd>`).join("")}
function workflowFor(node){return state.graph?.workflows?.find(w=>w.instance_id===node.workflow_instance_id)}
function endeavorCount(item){return item.conversation_count??item.session_count??item.conversations?.length??0}
function pendingForNode(node){return(state.graph?.pending_actions||[]).filter(action=>action.workspace_node_id===node.node_id||(node.config?.pending_action_ids||[]).includes(action.action_id))}
function proposalSummary(action){return action.verdict?.objection||action.verdict?.reason||(action.assessment?.reasons||[])[0]||action.proposal?.intended_effect||"This proposed action needs an operator decision."}
function renderApprovalInbox(){const pending=state.graph?.pending_actions||[],button=$("#approvalInbox");button.hidden=!pending.length;button.textContent=pending.length===1?"1 approval required":`${pending.length} approvals required`;button.onclick=()=>{const action=pending[0],node=state.graph?.nodes?.find(item=>item.node_id===action.workspace_node_id)||state.graph?.nodes?.find(item=>(item.config?.pending_action_ids||[]).includes(action.action_id));if(node){selectMessage(node.node_id);requestAnimationFrame(()=>focusNode(node.node_id))}else toast("Select the held assistant message to review this action")}}
function renderRail(){const selectedEnd=state.graph?.endeavor?.id,selectedConv=state.graph?.conversation?.session;$("#endeavorList").innerHTML=state.endeavors.map(item=>{const conversations=item.conversations||[],active=item.id===selectedEnd;return`<div class="rail-group ${active?"active":""}"><button class="rail-card ${active?"active":""}" data-endeavor="${esc(item.id)}"><strong>${esc(item.title)}</strong><span class="status-line"><i class="status-dot ${esc(item.status)}"></i>ENDEAVOR · contains ${endeavorCount(item)} conversation${endeavorCount(item)===1?"":"s"}</span></button><div class="conversation-children">${conversations.map(conversation=>`<button class="rail-conversation ${conversation.session===selectedConv?"active":""}" data-conversation="${esc(conversation.session)}" data-parent-endeavor="${esc(item.id)}"><strong>${esc(conversation.title)}</strong><span>CONVERSATION · ${conversation.node_count||0} nodes${conversation.updated_at?` · ${timeLabel(conversation.updated_at)}`:""}</span></button>`).join("")||'<span style="font-size:8px;color:var(--muted)">No conversations yet</span>'}</div></div>`}).join("")||'<div class="rail-card"><span>No endeavors yet</span></div>';const section=$("#unassignedSection");section.hidden=!state.unassigned.length;$("#unassignedList").innerHTML=state.unassigned.map(item=>`<button class="rail-conversation" data-history-conversation="${esc(item.session)}"><strong>${esc(item.title)}</strong><span>CONVERSATION · archived · ${timeLabel(item.updated_at)}</span></button>`).join("");const stores=state.graph?.stores||[];$("#storeList").innerHTML=stores.map(store=>`<button class="store-chip" data-store="${esc(store.store_id)}"><i></i><span>${esc(store.name)} · ${store.record_count} records</span></button>`).join("")||'<div class="store-chip"><i></i><span>No stores configured</span></div>';document.querySelectorAll("[data-endeavor]").forEach(el=>el.onclick=()=>load({endeavor:el.dataset.endeavor}));document.querySelectorAll("[data-conversation]").forEach(el=>el.onclick=()=>load({endeavor:el.dataset.parentEndeavor,conversation:el.dataset.conversation}));document.querySelectorAll("[data-history-conversation]").forEach(el=>el.onclick=()=>load({historyConversation:el.dataset.historyConversation}));document.querySelectorAll("[data-store]").forEach(el=>el.onclick=()=>showStore(el.dataset.store))}
function graphLayout(){const nodes=state.graph?.nodes||[],edges=state.graph?.edges||[];if(!nodes.length)return{positions:{},width:900,height:600};const incoming=Object.fromEntries(nodes.map(n=>[n.node_id,0]));edges.forEach(e=>{if(e.target_id in incoming)incoming[e.target_id]++});const depth={},queue=nodes.filter(n=>!incoming[n.node_id]).map(n=>n.node_id);queue.forEach(id=>depth[id]=0);while(queue.length){const id=queue.shift();edges.filter(e=>e.source_id===id).forEach(e=>{depth[e.target_id]=Math.max(depth[e.target_id]??0,(depth[id]??0)+1);incoming[e.target_id]--;if(incoming[e.target_id]<=0)queue.push(e.target_id)})}nodes.forEach((n,i)=>{if(depth[n.node_id]==null)depth[n.node_id]=i});const levels={};nodes.forEach(n=>(levels[depth[n.node_id]]??=[]).push(n));const positions={},maxLevel=Math.max(...Object.keys(levels).map(Number),0);Object.entries(levels).forEach(([d,items])=>{items.sort((a,b)=>a.ordinal-b.ordinal);const gap=470,total=(items.length-1)*gap;items.forEach((node,index)=>{positions[node.node_id]={x:node.position_x??(900-total/2+index*gap),y:node.position_y??(115+Number(d)*410)}})});return{positions,width:1800,height:Math.max(780,420+maxLevel*410)}}
function agentFor(id){return state.agents.find(agent=>agent.id===id)}
function workflowExecution(workflow){const detail=state.workflowExecutions[workflow.instance_id];return detail&&!detail.loading&&!detail.error?detail:(workflow.execution_summary||{})}
function workflowGraphLayout(workflow,compact=false){const nodes=workflow.graph?.nodes||[],edges=(workflow.graph?.edges||[]).filter(edge=>!edge.loop),incoming=new Map(nodes.map(node=>[node.id,0]));edges.forEach(edge=>incoming.set(edge.target,(incoming.get(edge.target)||0)+1));const entry=workflow.graph?.entry,queue=[entry,...nodes.filter(node=>node.id!==entry&&!incoming.get(node.id)).map(node=>node.id)],layer=new Map(entry?[[entry,0]]:[]),seen=new Set();while(queue.length){const id=queue.shift();if(!id||seen.has(id))continue;seen.add(id);edges.filter(edge=>edge.source===id).forEach(edge=>{layer.set(edge.target,Math.max(layer.get(edge.target)||0,(layer.get(id)||0)+1));incoming.set(edge.target,(incoming.get(edge.target)||0)-1);if(incoming.get(edge.target)<=0)queue.push(edge.target)})}nodes.forEach(node=>{if(!layer.has(node.id))layer.set(node.id,0)});const groups={};nodes.forEach(node=>(groups[layer.get(node.id)]??=[]).push(node));const W=compact?150:190,H=compact?54:72,X=compact?50:88,Y=compact?18:32,pad=compact?18:42,maxRows=Math.max(1,...Object.values(groups).map(group=>group.length)),maxLayer=Math.max(0,...layer.values()),content=maxRows*H+(maxRows-1)*Y,width=pad*2+(maxLayer+1)*W+maxLayer*X,height=Math.max(compact?122:360,pad*2+content),top=(height-content)/2,pos={};Object.entries(groups).forEach(([value,group])=>{const offset=(maxRows-group.length)*(H+Y)/2;group.forEach((node,index)=>pos[node.id]={x:pad+Number(value)*(W+X),y:top+offset+index*(H+Y),w:W,h:H})});return{pos,width,height}}
function workflowEdgePath(a,b,loop){const sx=a.x+a.w,sy=a.y+a.h/2,tx=b.x,ty=b.y+b.h/2;if(loop||tx<=sx){const lift=Math.min(sy,ty)-42;return`M${sx} ${sy} C${sx+38} ${lift},${tx-38} ${lift},${tx} ${ty}`}const bend=Math.max(28,(tx-sx)*.44);return`M${sx} ${sy} C${sx+bend} ${sy},${tx-bend} ${ty},${tx} ${ty}`}
function workflowGraphMarkup(workflow,compact=false){const nodes=workflow.graph?.nodes||[],edges=workflow.graph?.edges||[],execution=workflowExecution(workflow),observed=new Set(execution.observed_nodes||[]),transitions=new Set((execution.observed_transitions||[]).map(item=>item.source+">"+item.target)),current=execution.current_node||workflow.active_node,entry=state.expandedWorkflows[workflow.instance_id],selected=entry?.selectedNode,{pos,width,height}=workflowGraphLayout(workflow,compact),marker="wfArrow"+workflow.instance_id.replace(/[^a-zA-Z0-9]/g,""),steps=execution.model_steps||[],events=[...(execution.runtime_events||[]),...(execution.trace_events||[])];let svg=`<svg class="workflow-graph" viewBox="0 0 ${width} ${height}" role="group" aria-label="${esc(compact?"Workflow run preview with observed path":"Complete workflow graph with observed execution path")}"><defs><marker id="${marker}" viewBox="0 0 8 8" refX="7" refY="4" markerWidth="6" markerHeight="6" orient="auto"><path d="M0 0L8 4L0 8Z" fill="context-stroke"/></marker></defs>`;edges.forEach(edge=>{const a=pos[edge.source],b=pos[edge.target];if(!a||!b)return;const isObserved=transitions.has(edge.source+">"+edge.target),active=edge.source===current,caption=edge.label||edge.condition||(edge.loop?"retry":"continue"),midX=(a.x+a.w+b.x)/2,midY=(a.y+a.h/2+b.y+b.h/2)/2;svg+=`<path class="wg-edge ${edge.loop?"loop":""} ${isObserved?"observed":""} ${active?"active":""}" d="${workflowEdgePath(a,b,edge.loop)}" marker-end="url(#${marker})"/>${compact?"":`<text class="wg-edge-label" x="${midX}" y="${midY-6}" text-anchor="middle">${esc(short(caption,28))}</text>`}`});nodes.forEach(node=>{const p=pos[node.id],nodeSteps=steps.filter(step=>step.node_id===node.id).length,nodeEvents=events.filter(event=>event.node_id===node.id).length,status=node.id===current?(workflow.status==="awaiting_approval"||workflow.status==="awaiting_input"?"waiting":workflow.status==="failed"?"failed":"current"):observed.has(node.id)||node.runtime_status==="complete"?"observed":"",agent=agentFor(node.agent),count=nodeSteps?`${nodeSteps} LLM call${nodeSteps===1?"":"s"}`:nodeEvents?`${nodeEvents} event${nodeEvents===1?"":"s"}`:observed.has(node.id)?"observed stage":"not observed";svg+=`<g class="wg-node ${status} ${selected===node.id?"selected":""}" data-${compact?"":"overlay-"}workflow-node="${esc(node.id)}" data-${compact?"":"overlay-"}workflow="${esc(workflow.instance_id)}" transform="translate(${p.x} ${p.y})" tabindex="0" role="button" aria-label="${esc(node.label+" · "+count)}"><rect width="${p.w}" height="${p.h}" rx="9"/><path class="wg-accent" d="M0 9Q0 0 9 0H7V${p.h}H9Q0 ${p.h} 0 ${p.h-9}Z"/><text class="wg-kind" x="16" y="${compact?15:18}">${esc(short(node.type+(agent?" · "+agent.label:""),compact?25:33))}</text><text class="wg-label" x="16" y="${compact?32:41}">${esc(short(node.label,compact?23:28))}</text>${compact?"":`<text class="wg-count" x="16" y="59">${esc(count)}</text>`}</g>`});return svg+"</svg>"}
function miniWorkflow(workflow){if(!workflow)return"";const execution=workflow.execution_summary||{},nodes=workflow.graph?.nodes||[],expanded=Boolean(state.expandedWorkflows[workflow.instance_id]),observed=(execution.observed_nodes||[]).length;return`<div class="workflow-shell ${expanded?"expanded-source":""}" data-workflow-shell="${esc(workflow.instance_id)}"><div class="workflow-head"><span class="workflow-identity"><b>WORKFLOW RUN · ASSISTANT OUTPUT</b><small>same definition and observed path as Agent Graphs</small></span><div class="workflow-run-stats"><span>${observed}/${nodes.length} stages</span><span>${execution.model_step_count||0} LLM calls</span></div><button class="workflow-expand" data-workflow-control data-expand-workflow="${esc(workflow.instance_id)}" aria-label="Expand complete workflow run with ${nodes.length} stages">Expand run ↗</button></div><div class="workflow-run-mini">${workflowGraphMarkup(workflow,true)}</div></div>`}
function workflowDisplayName(workflow){const owner=state.graph?.nodes?.find(node=>node.node_id===workflow.owner_node_id),incoming=state.graph?.edges?.find(edge=>edge.target_id===workflow.owner_node_id),parent=state.graph?.nodes?.find(node=>node.node_id===incoming?.source_id),prompt=parent&&(parent.output_text||parent.input_text||parent.label);return prompt?`Response to · ${short(prompt,48)}`:owner?.label||workflow.flow_id||workflow.instance_id}
function workflowSource(instance){return Array.from(document.querySelectorAll("[data-workflow-shell]")).find(el=>el.dataset.workflowShell===instance)}
function workflowStageStatus(workflow,node,execution=workflowExecution(workflow)){const observed=(execution?.observed_nodes||[]).includes(node.id);return node.id===execution?.current_node?(execution.status||workflow.status):observed?"observed":node.runtime_status||"idle"}
function workflowStageDetail(workflow,node,execution){if(execution?.loading)return'<div class="workflow-loading">Loading the complete durable execution record…</div>';if(execution?.error)return`<div class="workflow-loading">${esc(execution.error)}</div>`;const agent=agentFor(node.agent),steps=(execution?.model_steps||[]).filter(step=>step.node_id===node.id),events=[...(execution?.runtime_events||[]),...(execution?.trace_events||[])].filter(event=>event.node_id===node.id),stageStatus=workflowStageStatus(workflow,node,execution),configuration={node:node.config||{},agent:agent?{id:agent.id,label:agent.label,role:agent.role,model_selector:agent.model_selector,family_rule:agent.family_rule,capabilities:agent.capabilities}:null};return`<span class="eyebrow">${esc(node.type)} stage</span><h3>${esc(node.label)}</h3><p class="stage-description">${esc(node.description||agent?.description||"Declared deterministic workflow boundary.")}</p><div class="workflow-run-overview"><span>${steps.length} LLM call${steps.length===1?"":"s"}</span><span>${events.length} recorded event${events.length===1?"":"s"}</span><span>${esc(stageStatus)}</span></div><dl class="stage-meta"><dt>Node</dt><dd>${esc(node.id)}</dd><dt>Identity</dt><dd>${esc(agent?.label||node.agent||"deterministic / system")}</dd><dt>Model rule</dt><dd>${esc(agent?.model_selector||"no model")}</dd><dt>Capabilities</dt><dd>${esc((node.capabilities||[]).join(", ")||"none")}</dd></dl><div class="stage-section"><strong>Configuration</strong><pre>${esc(JSON.stringify(configuration,null,2))}</pre></div><div class="stage-section"><strong>LLM input and output</strong>${steps.length?steps.map((step,index)=>`<details class="model-step" ${index===0?"open":""}><summary>${esc(step.model)} · ${esc(step.kind)} · ${step.tokens_in+step.tokens_out} tokens · $${Number(step.cost_usd||0).toFixed(5)}</summary><div class="model-step-grid"><label>Input<pre>${esc(JSON.stringify(step.input,null,2))}</pre></label><label>Output<pre>${esc(JSON.stringify(step.output,null,2))}</pre></label><label style="grid-column:1/-1">Call configuration and accounting<pre>${esc(JSON.stringify(step.configuration||{},null,2))}</pre></label></div></details>`).join(""):'<div class="runtime-event">No LLM call was recorded at this stage. It is deterministic, unvisited, or its legacy trace predates exchange capture.</div>'}</div><div class="stage-section"><strong>Observed stage events</strong>${events.length?events.map(event=>`<div class="runtime-event"><b>${esc(event.summary||event.kind)}</b><br>${esc(event.kind)} · ${esc(event.status||"recorded")}${event.model?` · ${esc(event.model)}`:""}${Object.keys(event.data||{}).length?`<details><summary>Event data</summary><pre>${esc(JSON.stringify({data:event.data,evidence:event.evidence||[]},null,2))}</pre></details>`:""}</div>`).join(""):'<div class="runtime-event">No event recorded for this stage.</div>'}</div>`}
async function loadWorkflowExecution(instance){const workflow=state.graph?.workflows?.find(item=>item.instance_id===instance),summary=workflow?.execution_summary||{},existing=state.workflowExecutions[instance];if(!workflow||existing?.loading||(existing&&!existing.error&&existing.task_id===summary.task_id))return;state.workflowExecutions[instance]={loading:true,task_id:summary.task_id};renderWorkflowOverlays();try{state.workflowExecutions[instance]=await api(`/admin/workspace/workflows/${encodeURIComponent(instance)}/execution`)}catch(error){state.workflowExecutions[instance]={error:error.message,task_id:summary.task_id}}renderWorkflowOverlays()}
function syncWorkflowOverlayStack(){
  document.querySelectorAll("[data-workflow-overlay]").forEach(el=>{const entry=state.expandedWorkflows[el.dataset.workflowOverlay];if(entry)el.style.zIndex=entry.z});
  document.querySelectorAll("[data-switch-workflow]").forEach(el=>el.classList.toggle("current",el.dataset.switchWorkflow===state.activeWorkflowOverlay));
}
function bringWorkflowOverlayFront(instance){const entry=state.expandedWorkflows[instance];if(!entry||entry.closing)return;entry.z=++state.overlayZ;state.activeWorkflowOverlay=instance;syncWorkflowOverlayStack()}
function clearWorkflowCollapse(instance){const entry=state.expandedWorkflows[instance];if(entry?.collapseTimer){clearTimeout(entry.collapseTimer);entry.collapseTimer=null}}
function scheduleWorkflowCollapse(instance){const entry=state.expandedWorkflows[instance];if(!entry||entry.pinned||entry.closing||entry.interacting)return;clearWorkflowCollapse(instance);const grace=Math.max(0,(entry.collapseHoldUntil||0)-Date.now());entry.collapseTimer=setTimeout(()=>grace?scheduleWorkflowCollapse(instance):closeWorkflowOverlay(instance),grace||500)}
function closeWorkflowOverlay(instance){
  const entry=state.expandedWorkflows[instance];if(!entry||entry.closing)return;clearWorkflowCollapse(instance);entry.closing=true;
  const overlay=Array.from(document.querySelectorAll("[data-workflow-overlay]")).find(el=>el.dataset.workflowOverlay===instance),source=workflowSource(instance);
  if(overlay&&source){const from=overlay.getBoundingClientRect(),to=source.getBoundingClientRect();overlay.classList.add("closing");overlay.classList.remove("open");overlay.style.transform=`translate(${to.left-from.left}px,${to.top-from.top}px) scale(${Math.max(.04,to.width/from.width)},${Math.max(.04,to.height/from.height)})`;overlay.style.opacity="0"}
  entry.closeTimer=setTimeout(()=>{delete state.expandedWorkflows[instance];const remaining=Object.entries(state.expandedWorkflows).sort((a,b)=>b[1].z-a[1].z);state.activeWorkflowOverlay=remaining[0]?.[0]||null;renderGraph();wireNodeDrag();renderWorkflowOverlays()},320)
}
function toggleWorkflowPin(instance){const entry=state.expandedWorkflows[instance];if(!entry||entry.closing)return;entry.pinned=!entry.pinned;clearWorkflowCollapse(instance);bringWorkflowOverlayFront(instance);renderWorkflowOverlays()}
function expandWorkflowOverlay(instance,sourceElement){
  const workflow=state.graph?.workflows?.find(item=>item.instance_id===instance);if(!workflow)return;
  Object.entries(state.expandedWorkflows).forEach(([id,entry])=>{if(id!==instance&&!entry.pinned)scheduleWorkflowCollapse(id)});
  const source=sourceElement?.closest("[data-workflow-shell]")||workflowSource(instance),rect=source?.getBoundingClientRect();
  if(!state.expandedWorkflows[instance])state.expandedWorkflows[instance]={pinned:false,z:++state.overlayZ,sourceRect:rect?{left:rect.left,top:rect.top,width:rect.width,height:rect.height}:null,opened:false,closing:false,selectedNode:workflow.execution_summary?.current_node||workflow.active_node||workflow.execution_summary?.observed_nodes?.at(-1)||workflow.graph?.entry};
  else{clearWorkflowCollapse(instance);clearTimeout(state.expandedWorkflows[instance].closeTimer);state.expandedWorkflows[instance].closing=false;state.expandedWorkflows[instance].z=++state.overlayZ}
  state.activeWorkflowOverlay=instance;renderGraph();wireNodeDrag();renderWorkflowOverlays();loadWorkflowExecution(instance)
}
function wireWorkflowOverlayGeometry(overlay,entry){
  const host=$("#workflowOverlays"),instance=overlay.dataset.workflowOverlay,drag=overlay.querySelector("[data-workflow-drag]"),resize=overlay.querySelector("[data-workflow-resize]");
  drag.onpointerdown=e=>{
    if(e.button!==0||e.target.closest("button,.workflow-switcher"))return;e.preventDefault();clearWorkflowCollapse(instance);bringWorkflowOverlayFront(instance);entry.interacting=true;overlay.classList.add("dragging");
    const startLeft=overlay.offsetLeft,startTop=overlay.offsetTop,startX=e.clientX,startY=e.clientY;drag.setPointerCapture(e.pointerId);
    drag.onpointermove=move=>{if(!drag.hasPointerCapture(move.pointerId))return;const rect=overlay.getBoundingClientRect(),left=Math.max(8,Math.min(host.clientWidth-rect.width-8,startLeft+move.clientX-startX)),top=Math.max(8,Math.min(host.clientHeight-rect.height-8,startTop+move.clientY-startY));overlay.style.left=left+"px";overlay.style.top=top+"px";entry.position={left,top}};
    drag.onpointerup=drag.onpointercancel=end=>{if(drag.hasPointerCapture(end.pointerId))drag.releasePointerCapture(end.pointerId);entry.interacting=false;entry.collapseHoldUntil=Date.now()+900;overlay.classList.remove("dragging")}
  };
  resize.onpointerdown=e=>{
    if(e.button!==0)return;e.preventDefault();e.stopPropagation();clearWorkflowCollapse(instance);bringWorkflowOverlayFront(instance);entry.interacting=true;overlay.classList.add("resizing");
    const rect=overlay.getBoundingClientRect(),startWidth=rect.width,startHeight=rect.height,startX=e.clientX,startY=e.clientY,left=overlay.offsetLeft,top=overlay.offsetTop;entry.position={left,top};resize.setPointerCapture(e.pointerId);
    resize.onpointermove=move=>{if(!resize.hasPointerCapture(move.pointerId))return;const maxWidth=Math.max(360,host.clientWidth-left-8),maxHeight=Math.max(280,host.clientHeight-top-8),width=Math.max(Math.min(520,maxWidth),Math.min(maxWidth,startWidth+move.clientX-startX)),height=Math.max(Math.min(360,maxHeight),Math.min(maxHeight,startHeight+move.clientY-startY));overlay.style.width=width+"px";overlay.style.height=height+"px";entry.size={width,height}};
    resize.onpointerup=resize.onpointercancel=end=>{if(resize.hasPointerCapture(end.pointerId))resize.releasePointerCapture(end.pointerId);entry.interacting=false;entry.collapseHoldUntil=Date.now()+900;overlay.classList.remove("resizing")}
  }
}
function wireWorkflowGraphPan(canvas){if(!canvas)return;let gesture=null;canvas.onpointerdown=e=>{if(e.button!==0||e.target.closest(".wg-node"))return;gesture={id:e.pointerId,x:e.clientX,y:e.clientY,left:canvas.scrollLeft,top:canvas.scrollTop};canvas.setPointerCapture(e.pointerId);canvas.classList.add("panning")};canvas.onpointermove=e=>{if(!gesture||gesture.id!==e.pointerId)return;canvas.scrollLeft=gesture.left+gesture.x-e.clientX;canvas.scrollTop=gesture.top+gesture.y-e.clientY};canvas.onpointerup=canvas.onpointercancel=e=>{if(gesture?.id===e.pointerId){gesture=null;canvas.classList.remove("panning")}}}
function renderWorkflowOverlays(){
  const host=$("#workflowOverlays"),workflows=state.graph?.workflows||[],valid=new Set(workflows.map(workflow=>workflow.instance_id));
  Object.keys(state.expandedWorkflows).forEach(instance=>{if(!valid.has(instance)){clearWorkflowCollapse(instance);clearTimeout(state.expandedWorkflows[instance].closeTimer);delete state.expandedWorkflows[instance]}});
  const expanded=workflows.filter(workflow=>state.expandedWorkflows[workflow.instance_id]);if(!expanded.length){host.innerHTML="";state.activeWorkflowOverlay=null;return}
  if(!state.activeWorkflowOverlay||!state.expandedWorkflows[state.activeWorkflowOverlay])state.activeWorkflowOverlay=expanded.sort((a,b)=>state.expandedWorkflows[b.instance_id].z-state.expandedWorkflows[a.instance_id].z)[0].instance_id;
  const available=Math.max(360,host.clientWidth-32);
  host.innerHTML=expanded.map((workflow,index)=>{const entry=state.expandedWorkflows[workflow.instance_id],nodes=workflow.graph?.nodes||[],execution=state.workflowExecutions[workflow.instance_id]||workflow.execution_summary||{},width=Math.min(available,Math.max(720,available*.92)),height=Math.min(Math.max(380,host.clientHeight-32),650),left=Math.max(10,(host.clientWidth-width)/2),top=16+Math.min(index,2)*7,name=workflowDisplayName(workflow);if(!entry.selectedNode||!nodes.some(node=>node.id===entry.selectedNode))entry.selectedNode=execution.current_node||workflow.active_node||execution.observed_nodes?.at(-1)||workflow.graph?.entry;const selected=nodes.find(node=>node.id===entry.selectedNode)||nodes[0],observed=(execution.observed_nodes||[]).length,modelCount=execution.model_step_count||0,eventCount=(execution.runtime_event_count||0)+(execution.trace_event_count||0);return`<section class="workflow-overlay ${entry.closing?"closing":""}" data-workflow-overlay="${esc(workflow.instance_id)}" style="left:${left}px;top:${top}px;width:${width}px;height:${height}px;z-index:${entry.z}" aria-label="Expanded workflow run for ${esc(name)}"><header class="workflow-overlay-head"><div class="workflow-overlay-title"><span class="eyebrow">Complete workflow run · observed path highlighted</span><h2>${esc(name)}</h2><p>${observed}/${nodes.length} stages observed · ${modelCount} LLM calls · ${eventCount} events · ${esc(execution.status||workflow.status||"idle")}</p></div><div class="workflow-overlay-controls"><button class="quiet-btn overlay-pin" data-pin-workflow="${esc(workflow.instance_id)}" aria-pressed="${entry.pinned}" title="${entry.pinned?"Keep this workflow open":"Keep open while using another part of the workspace"}">${entry.pinned?"◆ Pinned":"◇ Pin"}</button><button class="icon-btn" data-close-workflow="${esc(workflow.instance_id)}" aria-label="Close expanded workflow">×</button></div><nav class="workflow-switcher" aria-label="Workflow instances in this conversation">${workflows.map(item=>{const open=state.expandedWorkflows[item.instance_id];return`<button data-switch-workflow="${esc(item.instance_id)}" class="${item.instance_id===state.activeWorkflowOverlay?"current":""}">${esc(short(workflowDisplayName(item),42))}${open?.pinned?" · pinned":open?" · open":" · open next"}</button>`}).join("")}</nav></header><div class="workflow-overlay-scroll"><div class="workflow-run-layout"><div class="workflow-run-canvas" data-workflow-canvas="${esc(workflow.instance_id)}">${workflowGraphMarkup(workflow,false)}</div><aside class="workflow-run-inspector" aria-label="Selected workflow stage details">${selected?workflowStageDetail(workflow,selected,execution):'<div class="workflow-loading">No workflow stages exist.</div>'}</aside></div></div></section>`}).join("");
  document.querySelectorAll("[data-workflow-overlay]").forEach(overlay=>{
    const instance=overlay.dataset.workflowOverlay,entry=state.expandedWorkflows[instance],head=overlay.querySelector(".workflow-overlay-head");head.dataset.workflowDrag=instance;head.title="Drag this header to move the workflow";head.querySelector(".eyebrow")?.insertAdjacentHTML("beforeend",'<span class="workflow-move-hint" data-workflow-move role="button" tabindex="0" aria-label="Drag workflow overlay">⠿ Drag</span>');overlay.insertAdjacentHTML("beforeend",`<button class="workflow-overlay-resize" data-workflow-resize="${esc(instance)}" aria-label="Resize workflow overlay" title="Drag to resize">◢</button>`);
    if(entry.size){const width=Math.max(Math.min(520,host.clientWidth-16),Math.min(host.clientWidth-16,entry.size.width)),height=Math.max(Math.min(360,host.clientHeight-16),Math.min(host.clientHeight-16,entry.size.height));entry.size={width,height};overlay.style.width=width+"px";overlay.style.height=height+"px"}
    if(entry.position){const rect=overlay.getBoundingClientRect(),left=Math.max(8,Math.min(host.clientWidth-rect.width-8,entry.position.left)),top=Math.max(8,Math.min(host.clientHeight-rect.height-8,entry.position.top));entry.position={left,top};overlay.style.left=left+"px";overlay.style.top=top+"px"}
    overlay.onpointerdown=()=>bringWorkflowOverlayFront(instance);overlay.onpointerenter=()=>clearWorkflowCollapse(instance);overlay.onpointerleave=()=>scheduleWorkflowCollapse(instance);wireWorkflowOverlayGeometry(overlay,entry);wireWorkflowGraphPan(overlay.querySelector("[data-workflow-canvas]"));if(entry.opened)overlay.classList.add("open");else{entry.opened=true;const source=entry.sourceRect,target=overlay.getBoundingClientRect();if(source){overlay.style.transform=`translate(${source.left-target.left}px,${source.top-target.top}px) scale(${Math.max(.04,source.width/target.width)},${Math.max(.04,source.height/target.height)})`}requestAnimationFrame(()=>requestAnimationFrame(()=>overlay.classList.add("open")))}
  });
  document.querySelectorAll("[data-pin-workflow]").forEach(button=>button.onclick=e=>{e.stopPropagation();toggleWorkflowPin(button.dataset.pinWorkflow)});document.querySelectorAll("[data-close-workflow]").forEach(button=>button.onclick=e=>{e.stopPropagation();closeWorkflowOverlay(button.dataset.closeWorkflow)});document.querySelectorAll("[data-switch-workflow]").forEach(button=>button.onclick=e=>{e.stopPropagation();const instance=button.dataset.switchWorkflow;state.expandedWorkflows[instance]?bringWorkflowOverlayFront(instance):expandWorkflowOverlay(instance,workflowSource(instance))});document.querySelectorAll("[data-overlay-workflow-node]").forEach(node=>{node.onclick=e=>{e.stopPropagation();const entry=state.expandedWorkflows[node.dataset.overlayWorkflow];if(entry)entry.selectedNode=node.dataset.overlayWorkflowNode;bringWorkflowOverlayFront(node.dataset.overlayWorkflow);selectWorkflowNode(node.dataset.overlayWorkflow,node.dataset.overlayWorkflowNode)};node.onkeydown=e=>{if(e.key==="Enter"||e.key===" "){e.preventDefault();node.click()}}});syncWorkflowOverlayStack()
}
function renderGraph(){
  const container=$("#nodes"),svg=$("#edgeLayer"),envelopes=$("#envelopes"),nodes=state.graph?.nodes||[],edges=state.graph?.edges||[],layout=graphLayout(),pos=layout.positions;
  $("#stage").style.height=layout.height+"px";svg.setAttribute("height",layout.height);
  if(!nodes.length){container.innerHTML="";svg.innerHTML="";envelopes.innerHTML="";applyTransform();return}
  const xs=nodes.map(n=>pos[n.node_id].x),ys=nodes.map(n=>pos[n.node_id].y),minX=Math.min(...xs)-55,maxX=Math.max(...xs)+475,minY=Math.min(...ys)-55,maxY=Math.max(...ys)+330;
  envelopes.innerHTML=`<div class="endeavor-envelope" style="left:${minX-35}px;top:${minY-40}px;width:${maxX-minX+70}px;height:${maxY-minY+80}px"><span class="envelope-title">Endeavor · ${esc(short(state.graph.endeavor.title,50))}</span></div><div class="conversation-envelope" style="left:${minX-14}px;top:${minY-12}px;width:${maxX-minX+28}px;height:${maxY-minY+34}px"><span class="envelope-title">Conversation · ${esc(short(state.graph.conversation.title,45))}</span></div>`;
  svg.innerHTML=edges.map(edge=>{const a=pos[edge.source_id],b=pos[edge.target_id];if(!a||!b)return"";const source=state.graph.nodes.find(node=>node.node_id===edge.source_id),x1=a.x+210,y1=a.y+(workflowFor(source)?350:190),x2=b.x+210,y2=b.y;const mid=(y1+y2)/2,stale=state.graph.nodes.find(n=>n.node_id===edge.target_id)?.status==="stale";return`<path class="conv-edge ${stale?"stale":""} ${Math.abs(x2-x1)>100?"branch":""}" d="M${x1},${y1} C${x1},${mid} ${x2},${mid} ${x2},${y2}"/><text class="edge-label" x="${(x1+x2)/2+6}" y="${mid-5}">${esc(edge.label)}</text>`}).join("");
  container.innerHTML=nodes.map(node=>{const p=pos[node.node_id],wf=workflowFor(node),approval=pendingForNode(node),copy=node.role==="assistant"?(node.output_text||"Waiting to run…"):(node.output_text||node.input_text),kindClass=node.kind!=="message"?"context-node":"";return`<article class="message-node ${esc(node.role)} ${kindClass} ${esc(node.status)} ${approval.length?"awaiting_approval":""} ${state.selected?.kind==="message"&&state.selected.id===node.node_id?"selected":""}" data-node="${esc(node.node_id)}" style="left:${p.x}px;top:${p.y}px"><div class="message-head"><span class="role-icon">${node.kind==="context"?"C":node.kind==="store_read"?"↙":node.kind==="store_write"?"↗":node.role==="user"?"Y":"A"}</span><span class="message-title"><strong>${esc(node.label)}</strong><span>${esc(node.kind)} · revision ${node.revision}</span></span>${approval.length?'<span class="approval-flag">Decision needed</span>':`<span class="state-badge ${esc(node.status)}">${esc(node.status.replace("_"," "))}</span>`}</div><div class="message-copy">${esc(short(copy,260))}</div><div class="message-meta"><span>${esc(node.node_id)}</span>${node.config?.score!=null?`<span>score ${Number(node.config.score).toFixed(2)}</span>`:""}${node.config?.cost_usd!=null?`<span>$${Number(node.config.cost_usd).toFixed(4)}</span>`:""}</div>${wf?miniWorkflow(wf):""}</article>`}).join("");
  document.querySelectorAll("[data-node]").forEach(el=>el.onclick=e=>{if(e.target.closest("[data-workflow-node],[data-workflow-control]"))return;selectMessage(el.dataset.node)});
  document.querySelectorAll("[data-workflow-node]").forEach(el=>el.onclick=e=>{e.stopPropagation();selectWorkflowNode(el.dataset.workflow,el.dataset.workflowNode)});
  document.querySelectorAll("[data-expand-workflow]").forEach(el=>el.onclick=e=>{e.stopPropagation();expandWorkflowOverlay(el.dataset.expandWorkflow,el)});
  applyTransform()
}
function applyTransform(){const stage=$("#stage");stage.style.transform=`translate(${state.panX}px,${state.panY}px) scale(${state.zoom})`;$("#zoomLabel").textContent=Math.round(state.zoom*100)+"%"}
function fit(){const layout=graphLayout(),vp=$("#viewport").getBoundingClientRect(),nodes=state.graph?.nodes||[];if(!nodes.length){state.zoom=.8;state.panX=20;state.panY=20;applyTransform();return}const p=layout.positions,x=nodes.map(n=>p[n.node_id].x),y=nodes.map(n=>p[n.node_id].y),minX=Math.min(...x)-100,maxX=Math.max(...x)+520,minY=Math.min(...y)-100,maxY=Math.max(...y)+360;state.zoom=Math.max(.28,Math.min(1,(vp.width-50)/(maxX-minX),(vp.height-50)/(maxY-minY)));state.panX=(vp.width-(maxX-minX)*state.zoom)/2-minX*state.zoom;state.panY=25-minY*state.zoom;applyTransform()}
function focusNode(id){const p=graphLayout().positions[id],vp=$("#viewport").getBoundingClientRect();if(!p)return;state.zoom=Math.max(state.zoom,.72);state.panX=vp.width/2-(p.x+210)*state.zoom;state.panY=vp.height/2-(p.y+125)*state.zoom;applyTransform()}
function selectMessage(id){const node=state.graph.nodes.find(n=>n.node_id===id);if(!node)return;state.selected={kind:"message",id};$(".app").classList.remove("console-empty");renderGraph();renderConsole();$("#branchNote").textContent=`branch from ${short(node.label,26)}`;if(state.zoom<.62)requestAnimationFrame(()=>focusNode(id))}
function selectWorkflowNode(instance,id){state.selected={kind:"workflow",instance,id};$(".app").classList.remove("console-empty");renderGraph();wireNodeDrag();renderWorkflowOverlays();renderConsole()}
function approvalPanel(action){const exact={tool:action.tool_name,arguments:action.proposal?.arguments,postcondition:action.proposal?.postcondition,invariants:action.proposal?.invariants,rollback:action.proposal?.rollback};return`<section class="approval-panel"><header><strong>Operator decision required</strong><span>${esc(action.risk||"review")}</span></header><p>${esc(proposalSummary(action))}</p><div class="approval-targets">${esc((action.proposal?.targets||[]).join(" · ")||"Target was not resolved")}</div><details open><summary>Exact action & safeguards</summary><pre>${esc(JSON.stringify(exact,null,2))}</pre></details><textarea id="approvalNote" placeholder="Record why this is safe, or tell the agent how to re-plan"></textarea><div class="approval-decisions"><button class="danger-btn" id="denyAction">Block & continue</button><button class="primary-btn" id="approveAction">Approve once & continue</button></div></section>`}
async function decideAction(action,decision){const note=$("#approvalNote")?.value.trim()||"";if(decision==="approve"&&!note){toast("Record the verified safeguard for this one-time approval",true);$("#approvalNote")?.focus();return}$("#approveAction")?.setAttribute("disabled","");$("#denyAction")?.setAttribute("disabled","");try{await api(`/admin/workspace/actions/${encodeURIComponent(action.action_id)}/decision`,{method:"POST",body:JSON.stringify({decision,note})});toast(decision==="approve"?"Approved once. This message is continuing.":"Action blocked. The agent is continuing with your guidance.");await reloadGraph()}catch(error){toast(error.message,true);await reloadGraph()}}
function renderMessageConsole(body,actions){const node=state.graph.nodes.find(item=>item.node_id===state.selected.id);if(!node){state.selected=null;return renderConsole()}const approval=pendingForNode(node)[0];$("#consoleTitle").textContent=`${node.label} · message`;$("#consoleSubtitle").textContent=approval?`${node.node_id} · execution is paused at Human approval`:`${node.node_id} · revision ${node.revision} · the same Markdown terminal edits this message`;actions.innerHTML=`${node.role==="assistant"&&["running","queued"].includes(node.status)?'<button class="quiet-btn" id="pauseSelected">Pause</button>':node.role==="assistant"&&["paused","awaiting_input","stale","failed"].includes(node.status)?'<button class="quiet-btn" id="resumeSelected">Resume</button>':""}<button class="quiet-btn" id="recalculateSelected">Recalculate descendants</button>`;body.innerHTML=`<div class="edit-panel message-edit-panel">${approval?approvalPanel(approval):""}<div class="node-editor-grid">${markdownEditorMarkup("nodeInput",node.role==="assistant"?"assistant.input.md":"message.input.md",node.input_text,"what enters this node")}${markdownEditorMarkup("nodeOutput",node.role==="assistant"?"assistant.output.md":"message.output.md",node.output_text,"what dependent nodes receive")}</div><div class="editor-footer"><button class="primary-btn" id="saveNode">Save & recalculate dependents</button><span style="color:var(--muted);font-size:9px">Prior values remain in the revision ledger.</span></div></div>`;body.querySelectorAll("[data-markdown-editor]").forEach(wireMarkdownEditor);$("#saveNode").onclick=saveMessage;$("#recalculateSelected").onclick=()=>recalculateNode(node.node_id);if($("#pauseSelected"))$("#pauseSelected").onclick=()=>nodeAction(node.node_id,"pause");if($("#resumeSelected"))$("#resumeSelected").onclick=()=>nodeAction(node.node_id,"resume");if(approval){$("#approveAction").onclick=()=>decideAction(approval,"approve");$("#denyAction").onclick=()=>decideAction(approval,"deny")}}
function renderConsole(){const body=$("#consoleBody"),actions=$("#consoleActions");body.classList.toggle("message-editor-mode",state.selected?.kind==="message");if(!state.selected){$("#consoleTitle").textContent="Node terminal";$("#consoleSubtitle").textContent="Select a node to inspect its complete input and output";$("#branchNote").textContent=(state.graph?.nodes||[]).length?"continues the latest message":"starts this conversation";body.innerHTML='<div class="empty-selection">Select a message or a nested workflow node.<br>Input and output are always editable here.</div>';actions.innerHTML="";return}if(state.selected.kind==="message")return renderMessageConsole(body,actions);const workflow=state.graph.workflows.find(w=>w.instance_id===state.selected.instance),node=workflow?.graph.nodes.find(n=>n.id===state.selected.id);if(!workflow||!node){state.selected=null;return renderConsole()}const stageStatus=workflowStageStatus(workflow,node);$("#consoleTitle").textContent=`Workflow · ${node.label}`;$("#consoleSubtitle").textContent=`${node.type} node inside message ${workflow.owner_node_id} · instance revision ${workflow.revision}`;actions.innerHTML='<button class="quiet-btn" id="addWorkflowNode">+ Add after</button><button class="danger-btn" id="deleteWorkflowNode">Delete node</button>';body.innerHTML=`<div class="render-panel"><div class="panel-label"><span>Workflow node</span><span>${esc(stageStatus)}</span></div><div class="rendered"><p><strong>${esc(node.label)}</strong></p><p>${esc(node.description||"No description")}</p><p style="color:var(--muted)">Type: ${esc(node.type)}<br>Agent: ${esc(node.agent||"deterministic / system")}<br>Capabilities: ${esc((node.capabilities||[]).join(", ")||"none")}</p></div></div><div class="edit-panel"><div class="workflow-editor"><label class="editor-field"><span>LABEL</span><input id="workflowLabel" value="${esc(node.label)}"><span>DESCRIPTION</span><textarea id="workflowDescription">${esc(node.description||"")}</textarea></label><label class="editor-field"><span>NODE CONFIGURATION <small>prompt, model, store, ensemble, or pause policy</small></span><textarea id="workflowConfig">${esc(JSON.stringify(node.config||{},null,2))}</textarea></label></div><div class="editor-footer"><button class="primary-btn" id="saveWorkflowNode">Save workflow node</button><label class="apply-global"><input type="checkbox" id="applyGlobal"> Apply this workflow edit globally</label></div></div>`;$("#saveWorkflowNode").onclick=saveWorkflowNode;$("#addWorkflowNode").onclick=()=>openWorkflowNodeModal(workflow.instance_id,node.id);$("#deleteWorkflowNode").onclick=deleteWorkflowNode}
async function saveMessage(){const id=state.selected.id;try{const data=await api(`/admin/workspace/nodes/${encodeURIComponent(id)}`,{method:"PATCH",body:JSON.stringify({input_text:$("#nodeInput").value,output_text:$("#nodeOutput").value,auto_recalculate:true})});toast(data.job?"Saved. Dependent nodes are recalculating.":"Saved.");await reloadGraph()}catch(e){toast(e.message,true)}}
async function recalculateNode(id){try{await api(`/admin/workspace/nodes/${encodeURIComponent(id)}/recalculate`,{method:"POST",body:"{}"});toast("Recalculation queued");await reloadGraph()}catch(e){toast(e.message,true)}}
async function nodeAction(id,action){try{await api(`/admin/workspace/nodes/${encodeURIComponent(id)}/${action}`,{method:"POST",body:"{}"});toast(action==="pause"?"Execution paused":"Execution resumed");await reloadGraph()}catch(e){toast(e.message,true)}}
async function saveWorkflowNode(){const instance=state.selected.instance,id=state.selected.id;let config;try{config=JSON.parse($("#workflowConfig").value||"{}")}catch{toast("Node configuration must be valid JSON",true);return}try{await api(`/admin/workspace/workflows/${encodeURIComponent(instance)}/nodes/${encodeURIComponent(id)}`,{method:"PATCH",body:JSON.stringify({label:$("#workflowLabel").value,description:$("#workflowDescription").value,config,apply_globally:$("#applyGlobal").checked})});toast($("#applyGlobal").checked?"Saved and applied globally":"Workflow instance saved");await reloadGraph()}catch(e){toast(e.message,true)}}
async function deleteWorkflowNode(){if(!confirm("Delete this workflow node? Its incoming and outgoing paths will be reconnected."))return;try{await api(`/admin/workspace/workflows/${encodeURIComponent(state.selected.instance)}/nodes/${encodeURIComponent(state.selected.id)}`,{method:"DELETE",body:JSON.stringify({apply_globally:$("#applyGlobal")?.checked||false})});state.selected={kind:"message",id:state.graph.workflows.find(w=>w.instance_id===state.selected.instance).owner_node_id};toast("Workflow node deleted");await reloadGraph()}catch(e){toast(e.message,true)}}
function openModal(title,fields,submit,action){$("#entityTitle").textContent=title;$("#entitySubmit").textContent=submit;$("#entityFields").innerHTML=fields;state.modalAction=action;$("#entityModal").showModal()}
async function createEndeavor(){
  if(state.creatingEndeavor)return;state.creatingEndeavor=true;$("#newEndeavor").disabled=true;$("#addEndeavor").disabled=true;
  try{const e=await api("/admin/workspace/endeavors",{method:"POST",body:JSON.stringify({create_conversation:true})});const conversation=e.conversation||await api(`/admin/workspace/endeavors/${encodeURIComponent(e.id)}/conversations`,{method:"POST",body:"{}"});await load({endeavor:e.id,conversation:conversation.session});toast("New endeavor ready — start with the work, not a title.");requestAnimationFrame(()=>$("#messageTerminalInput").focus())}catch(error){toast(error.message,true)}finally{state.creatingEndeavor=false;$("#newEndeavor").disabled=false;$("#addEndeavor").disabled=false}
}
async function createConversation(){
  if(state.creatingConversation)return;if(!state.graph?.endeavor)return toast("Create an endeavor first",true);state.creatingConversation=true;$("#addConversation").disabled=true;
  try{const endeavor=state.graph.endeavor.id,c=await api(`/admin/workspace/endeavors/${encodeURIComponent(endeavor)}/conversations`,{method:"POST",body:"{}"});await load({endeavor,conversation:c.session});toast("New conversation ready — start with the work, not a title.");requestAnimationFrame(()=>$("#messageTerminalInput").focus())}catch(error){toast(error.message,true)}finally{state.creatingConversation=false;$("#addConversation").disabled=false}
}
function beginEndeavorRename(){const endeavor=state.graph?.endeavor;if(!endeavor)return;$("#endeavorTitle").hidden=true;$("#endeavorRename").hidden=false;$("#endeavorRenameInput").value=endeavor.title||"";requestAnimationFrame(()=>{$("#endeavorRenameInput").focus();$("#endeavorRenameInput").select()})}
function cancelEndeavorRename(){$("#endeavorRename").hidden=true;$("#endeavorTitle").hidden=false}
async function renameEndeavor(){const endeavor=state.graph?.endeavor,title=$("#endeavorRenameInput").value.trim();if(!endeavor||!title)return toast("Endeavor title cannot be empty",true);try{await api(`/admin/workspace/endeavors/${encodeURIComponent(endeavor.id)}`,{method:"PATCH",body:JSON.stringify({title})});cancelEndeavorRename();toast("Endeavor renamed");await load({endeavor:endeavor.id,conversation:state.graph?.conversation?.session})}catch(error){toast(error.message,true)}}
function beginConversationRename(){const conversation=state.graph?.conversation;if(!conversation)return;$("#conversationTitle").hidden=true;$("#conversationRename").hidden=false;$("#conversationRenameInput").value=conversation.title||"";requestAnimationFrame(()=>{$("#conversationRenameInput").focus();$("#conversationRenameInput").select()})}
function cancelConversationRename(){$("#conversationRename").hidden=true;$("#conversationTitle").hidden=false}
async function renameConversation(){const conversation=state.graph?.conversation,title=$("#conversationRenameInput").value.trim();if(!conversation||!title)return toast("Conversation title cannot be empty",true);try{await api(`/admin/workspace/conversations/${encodeURIComponent(conversation.session)}`,{method:"PATCH",body:JSON.stringify({title})});cancelConversationRename();toast("Conversation renamed");await reloadGraph()}catch(error){toast(error.message,true)}}
function openStoreModal(){openModal("New knowledge store",'<label>Name<input id="entityName" required placeholder="Project decisions"></label><label>Description<textarea id="entityDescription" placeholder="What belongs in this store"></textarea></label><label>Adapter<select id="entityAdapter"><option value="sqlite-vector">Built-in local vector store</option><option value="external-vector">External vector store (operator connection)</option></select></label>',"Create store",async()=>{await api("/admin/workspace/stores",{method:"POST",body:JSON.stringify({name:$("#entityName").value,description:$("#entityDescription").value,adapter:$("#entityAdapter").value})});await reloadGraph()})}
function openContextModal(){const selected=state.selected?.kind==="message"?state.selected.id:null;openModal("Add conversation graph node",`<label>Node type<select id="entityType"><option value="context">Required prompt context</option><option value="store_read">Retrieve from a knowledge store</option><option value="store_write">Save output to a knowledge store</option><option value="checkpoint">Checkpoint</option></select></label><label>Label<input id="entityName" value="Required context"></label><label>Content or instruction<textarea id="entityDescription"></textarea></label><label>Knowledge store<select id="entityStore"><option value="">None</option>${(state.graph?.stores||[]).map(s=>`<option value="${esc(s.store_id)}">${esc(s.name)}</option>`).join("")}</select></label>`,"Add node",async()=>{await api(`/admin/workspace/conversations/${encodeURIComponent(state.graph.conversation.session)}/nodes`,{method:"POST",body:JSON.stringify({kind:$("#entityType").value,label:$("#entityName").value,input_text:$("#entityDescription").value,parent_id:selected,config:{store_id:$("#entityStore").value}})});await reloadGraph()})}
function openWorkflowNodeModal(instance,after){openModal("Add workflow node",`<label>Node type<select id="entityType"><option value="ensemble">Multi-model ensemble</option><option value="human_input">Pause for user input</option><option value="context">Required prompt context</option><option value="store_read">Retrieve from a vector store</option><option value="store_write">Save to a vector store</option><option value="verifier">Additional verifier</option></select></label><label>Label<input id="entityName" value="Multi-model union"></label><label>Initial configuration<textarea id="entityDescription">{"mode":"union","candidate_count":3,"candidate_mode":"diverse_models","temperatures":[0.2,0.7,1.0]}</textarea></label><label class="apply-global"><input type="checkbox" id="entityGlobal"> Apply globally</label>`,"Add workflow node",async()=>{let config={};try{config=JSON.parse($("#entityDescription").value||"{}")}catch{throw new Error("Configuration must be valid JSON")}const data=await api(`/admin/workspace/workflows/${encodeURIComponent(instance)}/nodes`,{method:"POST",body:JSON.stringify({type:$("#entityType").value,label:$("#entityName").value,after_node_id:after,config,apply_globally:$("#entityGlobal").checked})});state.selected={kind:"workflow",instance,id:data.added_node_id};await reloadGraph()})}
function showStore(id){const store=state.graph.stores.find(s=>s.store_id===id);if(store)toast(`${store.name}: ${store.record_count} records · ${store.adapter}`)}
$("#entityForm").addEventListener("submit",async e=>{if(e.submitter?.value==="cancel")return;if(!state.modalAction)return;e.preventDefault();try{await state.modalAction();$("#entityModal").close()}catch(err){toast(err.message,true)}})
async function reloadGraph(){if(!state.graph?.conversation)return load({});const data=await api(`/admin/workspace/conversations/${encodeURIComponent(state.graph.conversation.session)}`);state.graph=data;Object.keys(state.workflowExecutions).forEach(instance=>{const workflow=data.workflows?.find(item=>item.instance_id===instance);if(!workflow||state.workflowExecutions[instance].task_id!==workflow.execution_summary?.task_id)delete state.workflowExecutions[instance]});state.endeavors.forEach(endeavor=>(endeavor.conversations||[]).forEach(conversation=>{if(conversation.session===data.conversation.session)Object.assign(conversation,data.conversation,{node_count:(data.nodes||[]).length})}));renderAll();Object.keys(state.expandedWorkflows).forEach(loadWorkflowExecution)}
async function load(selection={}){const serial=++state.serial,priorSession=state.graph?.conversation?.session;try{const params=new URLSearchParams();if(selection.historyConversation)params.set("history_conversation_id",selection.historyConversation);else{if(selection.endeavor)params.set("endeavor_id",selection.endeavor);else if(state.graph?.endeavor?.id)params.set("endeavor_id",state.graph.endeavor.id);if(selection.conversation)params.set("conversation_id",selection.conversation);else if(!selection.endeavor&&state.graph?.conversation?.session)params.set("conversation_id",state.graph.conversation.session)}const data=await api("/admin/workspace/bootstrap?"+params);if(serial!==state.serial)return;state.endeavors=data.endeavors||[];state.unassigned=data.unassigned_conversations||[];state.graph=data.graph;if(priorSession&&priorSession!==state.graph?.conversation?.session){state.selected=null;cancelEndeavorRename();cancelConversationRename();Object.values(state.expandedWorkflows).forEach(entry=>{clearTimeout(entry.collapseTimer);clearTimeout(entry.closeTimer)});state.expandedWorkflows={};state.workflowExecutions={};state.activeWorkflowOverlay=null}state.models=data.models||[];state.flows=data.flows||[];state.agents=data.agents||[];state.executionLock=data.execution_lock||null;$("#backendLock").textContent=`${state.executionLock?.backend||"configured"} locked · inspect`;renderExecutionBoundary();renderAll();requestAnimationFrame(fit);$("#syncState").textContent="synced "+new Date().toLocaleTimeString([], {hour:"2-digit",minute:"2-digit"})}catch(e){$("#syncState").textContent="offline";toast(e.message,true)}}
function wireNodeDrag(){
  document.querySelectorAll("[data-node]").forEach(el=>{
    el.onpointerdown=e=>{
      if(e.button!==0||e.target.closest("[data-workflow-node],[data-workflow-control]"))return;
      const node=state.graph.nodes.find(item=>item.node_id===el.dataset.node),position=graphLayout().positions[el.dataset.node];
      if(!node||!position)return;
      state.dragNode={id:e.pointerId,node,el,startX:e.clientX,startY:e.clientY,x:position.x,y:position.y,moved:false};
      el.setPointerCapture(e.pointerId);
    };
    el.onpointermove=e=>{
      const drag=state.dragNode;if(!drag||drag.id!==e.pointerId||drag.el!==el)return;
      const dx=(e.clientX-drag.startX)/state.zoom,dy=(e.clientY-drag.startY)/state.zoom;
      if(Math.abs(dx)+Math.abs(dy)>4)drag.moved=true;
      if(drag.moved){el.style.left=(drag.x+dx)+"px";el.style.top=(drag.y+dy)+"px"}
    };
    el.onpointerup=el.onpointercancel=async e=>{
      const drag=state.dragNode;if(!drag||drag.id!==e.pointerId||drag.el!==el)return;
      state.dragNode=null;
      if(!drag.moved)return;
      const x=parseFloat(el.style.left),y=parseFloat(el.style.top);
      drag.node.position_x=x;drag.node.position_y=y;
      try{await api(`/admin/workspace/nodes/${encodeURIComponent(drag.node.node_id)}`,{method:"PATCH",body:JSON.stringify({position_x:x,position_y:y,auto_recalculate:false})});await reloadGraph()}catch(error){toast(error.message,true);await reloadGraph()}
    };
  });
}
function renderAll(){$(".app").classList.toggle("console-empty",!state.selected);renderRail();renderApprovalInbox();$("#endeavorTitleText").textContent=state.graph?.endeavor?.title||"Choose an endeavor";$("#conversationTitleText").textContent=state.graph?.conversation?.title||"conversation";renderGraph();wireNodeDrag();renderWorkflowOverlays();renderConversationWelcome();renderConsole();schedulePoll()}
function schedulePoll(){clearTimeout(state.poll);const active=(state.graph?.jobs||[]).some(job=>["queued","running"].includes(job.status));if(active)state.poll=setTimeout(()=>reloadGraph().catch(()=>{}),1000)}
async function sendMessage(text,parent=null){
  text=String(text||"").trim();if(!text||state.sending)return;if(!state.graph?.conversation)return toast("Create a conversation first",true);state.sending=true;$("#composer").classList.add("sending");
  try{const data=await api(`/admin/workspace/conversations/${encodeURIComponent(state.graph.conversation.session)}/messages`,{method:"POST",body:JSON.stringify({content:text,parent_id:parent})});$("#messageTerminalInput").value="";renderComposerPreview();state.selected={kind:"message",id:data.pair.assistant.node_id};toast("Message queued; its workflow is now visible inside the node.");await reloadGraph();requestAnimationFrame(fit)}catch(err){toast(err.message,true)}finally{state.sending=false;$("#composer").classList.remove("sending")}
}
$("#composer").onsubmit=e=>{e.preventDefault();const parent=state.selected?.kind==="message"?state.selected.id:null;sendMessage(composerPrompt(),parent)};
$("#newEndeavor").addEventListener("click",createEndeavor);$("#addEndeavor").addEventListener("click",createEndeavor);$("#addConversation").onclick=createConversation;$("#addStore").onclick=openStoreModal;$("#addContext").onclick=openContextModal;$("#railRefresh").onclick=()=>load({});
$("#endeavorTitle").onclick=beginEndeavorRename;$("#cancelEndeavorRename").onclick=cancelEndeavorRename;$("#endeavorRename").onsubmit=e=>{e.preventDefault();renameEndeavor()};
$("#conversationTitle").onclick=beginConversationRename;$("#cancelConversationRename").onclick=cancelConversationRename;$("#conversationRename").onsubmit=e=>{e.preventDefault();renameConversation()};
$("#backendLockButton").onclick=()=>{$("#executionModal").showModal();renderExecutionBoundary()};wireMarkdownEditor($("#composer [data-markdown-editor]"));document.addEventListener("selectionchange",()=>{const input=document.activeElement;if(input?.matches?.("[data-markdown-editor] textarea"))scheduleTerminalCursor(input)});window.addEventListener("resize",()=>{const input=document.activeElement;if(input?.matches?.("[data-markdown-editor] textarea"))scheduleTerminalCursor(input)});
$("#zoomIn").onclick=()=>{state.zoom=Math.min(1.5,state.zoom+.1);applyTransform()};$("#zoomOut").onclick=()=>{state.zoom=Math.max(.25,state.zoom-.1);applyTransform()};$("#fitGraph").onclick=fit;
const viewport=$("#viewport");viewport.onpointerdown=e=>{if(e.target!==viewport&&e.target!==$("#stage")&&e.target!==$("#edgeLayer"))return;state.panning={id:e.pointerId,x:e.clientX,y:e.clientY,panX:state.panX,panY:state.panY};viewport.setPointerCapture(e.pointerId);viewport.classList.add("panning")};viewport.onpointermove=e=>{if(!state.panning||state.panning.id!==e.pointerId)return;state.panX=state.panning.panX+e.clientX-state.panning.x;state.panY=state.panning.panY+e.clientY-state.panning.y;applyTransform()};viewport.onpointerup=viewport.onpointercancel=e=>{if(state.panning?.id===e.pointerId){state.panning=null;viewport.classList.remove("panning")}};viewport.onwheel=e=>{if(!e.ctrlKey&&!e.metaKey)return;e.preventDefault();state.zoom=Math.max(.25,Math.min(1.5,state.zoom*(e.deltaY>0?.92:1.08)));applyTransform()};
document.addEventListener("pointerdown",e=>{if(e.target.closest("[data-workflow-overlay],[data-expand-workflow]"))return;Object.entries(state.expandedWorkflows).forEach(([instance,entry])=>{if(!entry.pinned)scheduleWorkflowCollapse(instance)})});
document.addEventListener("keydown",e=>{if(e.key!=="Escape")return;if(!$("#endeavorRename").hidden){cancelEndeavorRename();return}if(!$("#conversationRename").hidden){cancelConversationRename();return}if(state.activeWorkflowOverlay)closeWorkflowOverlay(state.activeWorkflowOverlay)});
let resize=null;$("#consoleHandle").onpointerdown=e=>{resize={id:e.pointerId,y:e.clientY,h:$(".console").getBoundingClientRect().height};e.target.setPointerCapture(e.pointerId)};$("#consoleHandle").onpointermove=e=>{if(!resize||resize.id!==e.pointerId)return;const h=Math.max(150,Math.min(innerHeight*.65,resize.h+resize.y-e.clientY));document.documentElement.style.setProperty("--console-h",h+"px")};$("#consoleHandle").onpointerup=$("#consoleHandle").onpointercancel=()=>resize=null;
new ResizeObserver(()=>{applyTransform();renderWorkflowOverlays()}).observe(viewport);load({});
// Trace-write-path SSE channel: reload the open conversation when one of its
// events commits, instead of blind interval polling. schedulePoll() remains
// only as the fallback for in-flight jobs if the stream drops.
let reloadQueued=false;
function queueReload(delay=300){if(reloadQueued||!state.graph?.conversation)return;reloadQueued=true;setTimeout(()=>{reloadQueued=false;reloadGraph().catch(()=>{})},delay)}
const live=new EventSource("/admin/events/stream");
live.onmessage=e=>{try{const ev=JSON.parse(e.data);if(!state.graph?.conversation||ev.session===state.graph.conversation.session||ev.session==="-")queueReload()}catch{queueReload()}};
live.onerror=()=>{};
</script>
</body>
</html>'''
