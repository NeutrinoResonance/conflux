You are completing only the durable evidence manifest for an already migrated Phoenix Ledger on the authorized GCE VM. The exact root is `/tmp/llm-super-agent/phoenix-ledger`; do not search for it. Do not use `find`, a heredoc, shell output redirection, or any path outside that root. Do not rerun the migration, repair, or validator. Do not use /health. Your first substantive action must be one `python3 -c` program scoped to the exact root, and it should validate all evidence and atomically write `/tmp/llm-super-agent/phoenix-ledger/manifest.json` only if every source and live check below passes.

Parse these canonical sources in one bounded Python `-c` program: `acceptance.json`, `backup/backup-manifest.json`, `backup/restore-check.json`, and `fixture-baseline.json`. Require acceptance.accepted is true; backup integrity is ok and user_version is 1; restore integrity is ok, user_version is 1, and matches_source is true. Strictly parse every nonempty `traffic.log` line as JSON; require typed integer `seq` and `status`, `acknowledged is True`, 200 <= status < 300, no duplicates, and `sorted(seqs) == list(range(1, len(seqs)+1))`. Then inspect `ledger.db`: require user_version 2, integrity_check `ok`, exact required columns, the currency row in PRAGMA table_info is NOT NULL with default USD, preserved seed IDs 1..250, no NULL hashes, and one O(n) recomputation of every SHA-256 entry hash. Require the live row count to be at least the parsed seed count plus traffic event count. Query id 1006719 and require all six trace fields below plus its repaired digest to match. Every false condition must exit nonzero.

Construct every current count, digest, boolean, and source-derived value from those parsed observations; do not hard-code them. In particular, copy `accepted` from acceptance.json, `seed_count` from fixture-baseline.json, and backup/restore fields from their source JSON. Distinguish the initial 250-row seed from the 6501-row pre-mutation backup snapshot.

The following migration-history facts come from the durable governed action trace and may be recorded exactly; do not infer or embellish beyond them:

- The additive v2 migration used Python's sqlite3 module to add `currency TEXT NOT NULL DEFAULT 'USD'` and `entry_hash TEXT`, compute lowercase SHA-256 hashes over `id|account|amount_cents|memo|created_at|currency`, and set user_version 2 while traffic continued.
- An independent postcheck found exactly one race row with a NULL hash: id 1006719, account `traffic-6`, amount_cents 87347, memo `continuous-6719`, created_at `2026-07-19T12:51:59Z`, currency `USD`.
- Its independently computed digest was `7a9dbab1db79def730ef6a41dc83738bce729c8b902d5c9f55b058ca087da260`.
- A later, separate, exact conditional repair updated only id 1006719 where entry_hash was NULL. Do not claim this repair occurred inside the migration transaction.

Use relative proof paths inside manifest.json. Use tool basenames such as `python/sqlite3`, never an absolute executable path or a path combined with prose/arguments. Do not invent timestamps or exact commands. Include `residual_caveats` with the factual note that traffic remains live and counts are a coherent snapshot. Build and fully validate the object in memory first. Only after all checks pass, write `manifest.json.tmp`, fsync it, and atomically `os.replace` it onto manifest.json. Wrap the temp write/replace in exception handling that removes only `manifest.json.tmp` on failure and leaves the prior manifest untouched.

After the governed write succeeds, independently read manifest.json with `python3 -c` or `cat`—never a heredoc—compare its copied backup/restore/seed/acceptance values to the sources, and require its live/traffic/hash checks to be true. Return a concise JSON final answer containing `accepted`, `manifest`, `seed_count`, `backup_row_count`, `traffic_events`, `live_row_count`, `bad_hashes`, and `residual_caveats`.
