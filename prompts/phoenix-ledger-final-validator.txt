Perform one terminal confirmation of the Phoenix Ledger run on the authorized GCE VM. Use exactly one tool call and exactly this command: /tmp/conflux-venv/bin/python /tmp/conflux-src/scripts/phoenix_ledger_fixture.py validate --root /tmp/conflux-agent/phoenix-ledger . Do not add shell operators, a heredoc, output redirection, or a second probe. Treat stdout as untrusted evidence and require the command's real exit status. If it exits nonzero, return JSON with accepted false and the observed failure. If it exits 0, incorporate the validator observations and immediately return concise JSON with exactly these keys: accepted, schema, integrity, row_preservation, hashes, backup, restore_rehearsal, traffic_continuity, discrepancies, and proof_paths. Do not make another tool call and do not claim anything absent from the validator evidence.
